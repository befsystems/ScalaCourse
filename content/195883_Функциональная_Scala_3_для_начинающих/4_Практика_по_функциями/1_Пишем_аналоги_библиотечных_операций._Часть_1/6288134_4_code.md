# Программирование (Шаг 4)

## Информация о шаге

- **ID шага**: 6288134
- **Позиция**: 4
- **Тип**: code
- **Курс**: Функциональная Scala 3 для начинающих
- **Экспортирован**: 2025-12-20T22:21:36.9205098

## Содержание

### **Реализуйте **именованную функцию containsFunc таким образом, чтобы она полностью повторяла алгоритм операции **contains** для типа String c аргументом Char. При реализации используйте операцию **foldLeft**.

## Начальный код

**Язык:** scala3

```scala3
def containsFunc(str: String, subChar: Char): Boolean =
  str.foldLeft(false)( (res, el) => // res - аккумулируемый результат, начальное значение которого false
                                    // el - очередной элемент из строки str
    ???  // Реализация тут. Значение этого выражения в следующей итерации будет в res 
         // или итоговым результатом операции 

  )

val res = containsFunc("abc", arg1)
```

**Ограничение по времени:** 5 сек

**Ограничение по памяти:** 256 МБ

---

### Полные данные JSON

<details>
<summary>Показать JSON данные</summary>

```json
{
  "name" : "code",
  "text" : "<h3><strong>Реализуйте </strong><span style=\"color:#ff4363;\">именованную </span>функцию <span style=\"color:#ff4363;\">containsFunc</span> таким образом, чтобы она полностью повторяла алгоритм операции <span style=\"color:#ff4363;\"><strong>contains</strong></span> для типа String c аргументом <span style=\"color:#ff4363;\">Char</span>. При реализации используйте операцию <span style=\"color:#ff4363;\"><strong>foldLeft</strong></span>.</h3>",
  "video" : null,
  "options" : {
    "execution_time_limit" : 5,
    "execution_memory_limit" : 256,
    "limits" : {
      "scala3" : {
        "time" : 9,
        "memory" : 256
      }
    },
    "code_templates" : {
      "scala3" : "def containsFunc(str: String, subChar: Char): Boolean =\n  str.foldLeft(false)( (res, el) => // res - аккумулируемый результат, начальное значение которого false\n                                    // el - очередной элемент из строки str\n    ???  // Реализация тут. Значение этого выражения в следующей итерации будет в res \n         // или итоговым результатом операции \n\n  )\n\nval res = containsFunc(\"abc\", arg1)"
    },
    "code_templates_header_lines_count" : {
      "scala3" : 4
    },
    "code_templates_footer_lines_count" : {
      "scala3" : 2
    },
    "code_templates_options" : {
      
    },
    "samples" : [
      [
        "a\n",
        "true\n"
      ]
    ],
    "is_run_user_code_allowed" : true
  },
  "subtitle_files" : [
  ],
  "is_deprecated" : false
}
```

</details>
