# Программирование (Шаг 10)

## Информация о шаге

- **ID шага**: 7368196
- **Позиция**: 10
- **Тип**: code
- **Курс**: Функциональная Scala 3 для начинающих
- **Экспортирован**: 2025-12-20T22:21:36.9299335

## Содержание

### **Реализуйте **анонимную функцию **replace**Func таким образом, чтобы она полностью повторяла алгоритм операции **replace** для типа String. При реализации используйте операцию **foldLeft**.

## Начальный код

**Язык:** scala3

```scala3
val replaceFunc: (String, Char, Char) => String =
  (str: String, oldChar: Char, newChar: Char) =>
    str.foldLeft("")( (acc, el) =>
      ???  // Реализация тут
    )  

val res = replaceFunc(arg1, arg2, arg3)
```

**Ограничение по времени:** 5 сек

**Ограничение по памяти:** 256 МБ

---

### Полные данные JSON

<details>
<summary>Показать JSON данные</summary>

```json
{
  "name" : "code",
  "text" : "<h3><strong>Реализуйте </strong><span style=\"color:#ff4363;\">анонимную </span>функцию&nbsp;<span style=\"color:#ff4363;\"><strong>replace</strong>Func </span>таким образом, чтобы она полностью повторяла алгоритм операции <span style=\"color:#ff4363;\"><strong>replace</strong> </span>для типа String. При реализации используйте операцию <span style=\"color:#ff4363;\"><strong>foldLeft</strong></span>.</h3>",
  "video" : null,
  "options" : {
    "execution_time_limit" : 5,
    "execution_memory_limit" : 256,
    "limits" : {
      "scala3" : {
        "time" : 9,
        "memory" : 256
      }
    },
    "code_templates" : {
      "scala3" : "val replaceFunc: (String, Char, Char) => String =\n  (str: String, oldChar: Char, newChar: Char) =>\n    str.foldLeft(\"\")( (acc, el) =>\n      ???  // Реализация тут\n    )  \n\nval res = replaceFunc(arg1, arg2, arg3)"
    },
    "code_templates_header_lines_count" : {
      "scala3" : 6
    },
    "code_templates_footer_lines_count" : {
      "scala3" : 2
    },
    "code_templates_options" : {
      
    },
    "samples" : [
      [
        "abc b d\n",
        "adc\n"
      ]
    ],
    "is_run_user_code_allowed" : true
  },
  "subtitle_files" : [
  ],
  "is_deprecated" : false
}
```

</details>
