# Программирование (Шаг 3)

## Информация о шаге

- **ID шага**: 5478417
- **Позиция**: 3
- **Тип**: code
- **Курс**: Функциональная Scala 3 для начинающих
- **Экспортирован**: 2026-01-10T10:17:12.7274387

## Содержание

### Добавьте к вызову функции третий аргумент в виде функционального выражения, чтобы функция выполняла операцию сложения, если первый аргумент отрицательный, или операцию вычитания, если первый аргумент положительный или ноль.

## Начальный код

**Язык:** scala3

```scala3
// Определение функции с двумя группами параметров
def calculate(param1: Int, param2: Int)(param3: (Int, Int) => Int): Int = 
  param3(param1,param2)

// Вызов функции
val res = calculate(arg1, arg2) // еще нужен третий аргумент с функциональным типом
```

**Ограничение по времени:** 5 сек

**Ограничение по памяти:** 256 МБ

---

### Полные данные JSON

<details>
<summary>Показать JSON данные</summary>

```json
{
  "name" : "code",
  "text" : "<h3>Добавьте к вызову функции третий аргумент в виде функционального выражения, чтобы функция выполняла операцию сложения, если первый аргумент отрицательный, или операцию вычитания, если первый аргумент положительный или ноль.</h3>",
  "video" : null,
  "options" : {
    "execution_time_limit" : 5,
    "execution_memory_limit" : 256,
    "limits" : {
      "scala3" : {
        "time" : 9,
        "memory" : 256
      }
    },
    "code_templates" : {
      "scala3" : "// Определение функции с двумя группами параметров\ndef calculate(param1: Int, param2: Int)(param3: (Int, Int) => Int): Int = \n  param3(param1,param2)\n\n// Вызов функции\nval res = calculate(arg1, arg2) // еще нужен третий аргумент с функциональным типом"
    },
    "code_templates_header_lines_count" : {
      "scala3" : 4
    },
    "code_templates_footer_lines_count" : {
      "scala3" : 2
    },
    "code_templates_options" : {
      
    },
    "samples" : [
      [
        "8 11\n",
        "-3\n"
      ]
    ],
    "is_run_user_code_allowed" : true
  },
  "subtitle_files" : [
  ],
  "is_deprecated" : false
}
```

</details>
