# Программирование (Шаг 5)

## Информация о шаге

- **ID шага**: 5264992
- **Позиция**: 5
- **Тип**: code
- **Курс**: Функциональная Scala 3 для начинающих
- **Экспортирован**: 2025-12-20T22:21:26.0976684

## Содержание

### Сначала исследуйте работу библиотечной функции **tan**. См. первое задание.

### **Реализуйте **функцию **tanX **таким образом, чтобы она полностью повторяла алгоритм функции **tan**, и замените функцию **tan** на функцию **tanX** в строке вызова функции**. **

### При реализации функции **tanX** не используйте функцию **tan**. Можно использовать **другие** функции стандартной библиотеки и любые известные вам языковые конструкции.

### После трех неудачных попыток реализации откроется пример верного решения.

## Начальный код

**Язык:** scala3

```scala3
def tanX(x: Double): Double = {
  ??? // реализация функции здесь   
}

// Функция усечения дробной части вещественного числа до трех знаков 
def truncation(x: Double): Double = 
  (x * 1000).toLong / 1000.0

val res = truncation(tan(arg1))  // замените на truncation(tanX(arg1))
```

**Ограничение по времени:** 5 сек

**Ограничение по памяти:** 256 МБ

---

### Полные данные JSON

<details>
<summary>Показать JSON данные</summary>

```json
{
  "name" : "code",
  "text" : "<h3>Сначала <span style=\"color: #ff4363;\">исследуйте</span> работу библиотечной функции <span style=\"color: #ff4363;\"><strong>tan</strong></span>. См. первое задание.</h3>\n\n<h3><strong><span style=\"color: #ff4363;\">Реализуйте</span> </strong>функцию <strong><span style=\"color: #ff4363;\">tanX</span> </strong>таким образом, чтобы она полностью повторяла алгоритм функции <span style=\"color: #ff4363;\"><strong>tan</strong></span>, и замените функцию <span style=\"color: #ff4363;\"><strong>tan</strong></span> на функцию <span style=\"color: #ff4363;\"><strong>tanX</strong></span> в строке вызова функции<strong>. </strong></h3>\n\n<h3>При реализации функции <span style=\"color: #ff4363;\"><strong>tanX</strong></span> не используйте функцию<span style=\"color: #ff4363;\"> <strong>tan</strong></span>. <span style=\"color: #ff4363;\">Можно</span> использовать <span style=\"color: #ff4363;\"><strong>другие</strong></span> функции стандартной библиотеки и любые известные вам языковые конструкции.</h3>\n\n<h3>После трех неудачных попыток реализации откроется пример верного решения.</h3>",
  "video" : null,
  "options" : {
    "execution_time_limit" : 5,
    "execution_memory_limit" : 256,
    "limits" : {
      "scala3" : {
        "time" : 9,
        "memory" : 256
      }
    },
    "code_templates" : {
      "scala3" : "def tanX(x: Double): Double = {\n  ??? // реализация функции здесь   \n}\n\n// Функция усечения дробной части вещественного числа до трех знаков \ndef truncation(x: Double): Double = \n  (x * 1000).toLong / 1000.0\n\nval res = truncation(tan(arg1))  // замените на truncation(tanX(arg1))"
    },
    "code_templates_header_lines_count" : {
      "scala3" : 4
    },
    "code_templates_footer_lines_count" : {
      "scala3" : 2
    },
    "code_templates_options" : {
      
    },
    "samples" : [
      [
        "0.5\n",
        "0.546\n"
      ]
    ],
    "is_run_user_code_allowed" : true
  },
  "subtitle_files" : [
  ],
  "is_deprecated" : false
}
```

</details>
