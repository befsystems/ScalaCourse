# Программирование (Шаг 3)

## Информация о шаге

- **ID шага**: 5264426
- **Позиция**: 3
- **Тип**: code
- **Курс**: Функциональная Scala 3 для начинающих
- **Экспортирован**: 2026-01-10T10:17:22.7315519

## Содержание

### Сначала исследуйте работу библиотечной функции **min**. См. первое задание.

### **Реализуйте **функцию **minX **таким образом, чтобы она полностью повторяла алгоритм функции **min**, и замените функцию **min **на функцию **minX** в строке вызова функции**. **

### При реализации функции **minX** не используйте функцию **min**. Можно использовать **другие** функции стандартной библиотеки и любые известные вам языковые конструкции.

### После трех неудачных попыток реализации откроется пример верного решения.

## Начальный код

**Язык:** scala3

```scala3
def minX(x: Int, y: Int): Int = {
  ??? // реализация функции здесь   
}

val res = min(arg1, arg2)  // замените на minX(arg1, arg2)
```

**Ограничение по времени:** 5 сек

**Ограничение по памяти:** 256 МБ

---

### Полные данные JSON

<details>
<summary>Показать JSON данные</summary>

```json
{
  "name" : "code",
  "text" : "<h3>Сначала <span style=\"color: #ff4363;\">исследуйте</span> работу библиотечной функции <span style=\"color: #ff4363;\"><strong>min</strong></span>. См. первое задание.</h3>\n\n<h3><strong><span style=\"color: #ff4363;\">Реализуйте</span> </strong>функцию <strong><span style=\"color: #ff4363;\">minX</span> </strong>таким образом, чтобы она полностью повторяла алгоритм функции <span style=\"color: #ff4363;\"><strong>min</strong></span>, и замените функцию <strong>min </strong>на функцию <span style=\"color: #ff4363;\"><strong>minX</strong></span> в строке вызова функции<strong>. </strong></h3>\n\n<h3>При реализации функции <span style=\"color: #ff4363;\"><strong>minX</strong></span> не используйте функцию <strong><span style=\"color: #ff4363;\">mi</span>n</strong>. <span style=\"color: #ff4363;\">Можно</span> использовать <span style=\"color: #ff4363;\"><strong>другие</strong></span> функции стандартной библиотеки и любые известные вам языковые конструкции.</h3>\n\n<h3>После трех неудачных попыток реализации откроется пример верного решения.</h3>",
  "video" : null,
  "options" : {
    "execution_time_limit" : 5,
    "execution_memory_limit" : 256,
    "limits" : {
      "scala3" : {
        "time" : 9,
        "memory" : 256
      }
    },
    "code_templates" : {
      "scala3" : "def minX(x: Int, y: Int): Int = {\n  ??? // реализация функции здесь   \n}\n\nval res = min(arg1, arg2)  // замените на minX(arg1, arg2)"
    },
    "code_templates_header_lines_count" : {
      "scala3" : 5
    },
    "code_templates_footer_lines_count" : {
      "scala3" : 2
    },
    "code_templates_options" : {
      
    },
    "samples" : [
      [
        "8 11\n",
        "8\n"
      ]
    ],
    "is_run_user_code_allowed" : true
  },
  "subtitle_files" : [
  ],
  "is_deprecated" : false
}
```

</details>
