# Программирование (Шаг 3)

## Информация о шаге

- **ID шага**: 5423595
- **Позиция**: 3
- **Тип**: code
- **Курс**: Функциональная Scala 3 для начинающих
- **Экспортирован**: 2025-12-20T22:21:12.7066459

## Содержание

### Доделайте рекурсивную функцию переворачивания (реверсирование) строки.

Вы можете для контроля за промежуточными результатами, в каждом шаге выводить значения на печать командой `println.` Однако, перед отправкой на проверку необходимо все команды печати удалить.

## Начальный код

**Язык:** scala3

```scala3
def reverseString(str: String): String = {
    if str.isEmpty then 
      "" // Базовый случай: если строка пустая, вернуть пустую строку
    else
      // Рекурсивный случай: 
      //  1. Получить первый символ строки  -- выражение на прямом ходу рекурсии
      //  2. Получить хвост строки (строка без первого символа) -- выражение на прямом ходу рекурсии
      //  3. Сделать вызов функции reverseString с аргументом хвоста строки  -- рекурсивный вызов функции
      //  4. Соединить результат рекурсивного вызова с первым символом  -- выражение на обратном ходу рекурсии
      ??? // Здесь реализация
}
```

**Ограничение по времени:** 5 сек

**Ограничение по памяти:** 256 МБ

---

### Полные данные JSON

<details>
<summary>Показать JSON данные</summary>

```json
{
  "name" : "code",
  "text" : "<h3>Доделайте рекурсивную функцию переворачивания (реверсирование) строки.</h3>\n\n<p>Вы можете для контроля за промежуточными результатами, в каждом шаге выводить значения на печать командой <code>println.</code> Однако, перед отправкой на проверку необходимо все команды печати удалить.</p>",
  "video" : null,
  "options" : {
    "execution_time_limit" : 5,
    "execution_memory_limit" : 256,
    "limits" : {
      "scala3" : {
        "time" : 9,
        "memory" : 256
      }
    },
    "code_templates" : {
      "scala3" : "def reverseString(str: String): String = {\n    if str.isEmpty then \n      \"\" // Базовый случай: если строка пустая, вернуть пустую строку\n    else\n      // Рекурсивный случай: \n      //  1. Получить первый символ строки  -- выражение на прямом ходу рекурсии\n      //  2. Получить хвост строки (строка без первого символа) -- выражение на прямом ходу рекурсии\n      //  3. Сделать вызов функции reverseString с аргументом хвоста строки  -- рекурсивный вызов функции\n      //  4. Соединить результат рекурсивного вызова с первым символом  -- выражение на обратном ходу рекурсии\n      ??? // Здесь реализация\n}"
    },
    "code_templates_header_lines_count" : {
      "scala3" : 2
    },
    "code_templates_footer_lines_count" : {
      "scala3" : 2
    },
    "code_templates_options" : {
      
    },
    "samples" : [
      [
        "Hello, World!\n",
        "!dlroW ,olleH\n"
      ]
    ],
    "is_run_user_code_allowed" : true
  },
  "subtitle_files" : [
  ],
  "is_deprecated" : false
}
```

</details>
