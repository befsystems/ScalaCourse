# Материал (Шаг 9)

## Информация о шаге

- **ID шага**: 5450498
- **Позиция**: 9
- **Тип**: text
- **Курс**: Функциональная Scala 3 для начинающих
- **Экспортирован**: 2025-12-20T22:21:12.7135894

## Содержание

# Множественная рекурсия

Минимальная диаграмма **множественной** рекурсия выглядит следующим образом:

![Изображение](https://ucarecdn.com/17acef71-2b18-4a26-9487-4c805177a272/)

Множественность выражается в **неоднократном **последовательном вызове рекурсивной функции в **одной **вычислительной ветке. Такой тип рекурсии позволяет значительно упрощать реализацию сложных вычислительных алгоритмов, так как по существу, у них нет ограничений на сложность связей между рекурсивными вызовами, и алгоритм не нужно приспосабливать под ограничения рекурсии.

Пример вычисления числа Фибоначчи:

*F*(*n*)=*F*(*n*−1)+*F*(*n*−2), где 𝐹(0)=0 и 𝐹(1)=1

def fibonacci(n: Int): Int = {
if n <= 1 then
n // базовый случай: fib(0) = 0, fib(1) = 1
else
fibonacci(n - 1) + fibonacci(n - 2) // рекурсивный случай: fib(n) = fib(n - 1) + fib(n - 2)
}



Более сложный пример множественной рекурсии:

![Изображение](https://ucarecdn.com/77a7d7e7-aa3e-4bd3-a0b2-0d8076f8f708/)

Вступая в мир множественной рекурсии мы уже двумя ногами попадаем в царство логики и математики, что требует усердия в осознании происходящего. Очень много возможностей и альтернатив. Любой алгоритм можно реализовать несколькими способами, и он может стать или супер наглядным или непостижимым для понимания. Поэтому множественная рекурсия является самым тонким и красивым способом построения вычислений, и является настоящей волшебной палочкой профессионалов.

Пример вычисления количества способов размена заданной суммы денег с использованием заданных монет.:

// money - сумма, coins - список монет номиналом от 1 до 9 в строке
def countChange(money: Int, coins: String): Int = {
if money == 0 then
1 // Базовый случай: если сумма денег равна 0, то есть один способ размена - ничего не выбирать
else if money < 0 || coins.isEmpty then
0 // Базовый случай: если сумма денег меньше 0 или монет нет, то способов размена нет
else
countChange(money, coins.tail) + countChange(money - coins.head.toString.toInt, coins) // Рекурсивный случай: вычисляем количество способов размена
}



Более полная диаграмма множественной рекурсии:

![Изображение](https://ucarecdn.com/234ec2a3-4eda-40a9-bd08-11b89cc4feaa/)

---

### Полные данные JSON

<details>
<summary>Показать JSON данные</summary>

```json
{
  "name" : "text",
  "text" : "<h1 style=\"text-align:center;\">Множественная рекурсия</h1>\n\n<p>Минимальная диаграмма <strong>множественной</strong> рекурсия выглядит следующим образом:</p>\n\n<p><img alt=\"\" height=\"302\" name=\"Диаграмма без названия.drawio(10).png\" src=\"https://ucarecdn.com/17acef71-2b18-4a26-9487-4c805177a272/\" width=\"937\"></p>\n\n<p>Множественность выражается в <strong>неоднократном </strong>последовательном вызове рекурсивной функции в <strong>одной </strong>вычислительной ветке. Такой тип рекурсии позволяет значительно упрощать реализацию сложных вычислительных алгоритмов, так как по существу, у них нет ограничений на сложность связей между рекурсивными вызовами, и алгоритм не нужно приспосабливать под ограничения рекурсии.</p>\n\n<p>Пример вычисления числа Фибоначчи:</p>\n\n<p style=\"background-color:#ffffff; border:0px solid #e3e3e3; color:#0d0d0d; font-size:16px; text-align:start;\"><em style=\"border:0px solid currentcolor;\">F</em>(<em style=\"border:0px solid currentcolor;\">n</em>)=<em style=\"border:0px solid currentcolor;\">F</em>(<em style=\"border:0px solid currentcolor;\">n</em>−1)+<em style=\"border:0px solid currentcolor;\">F</em>(<em style=\"border:0px solid currentcolor;\">n</em>−2), где 𝐹(0)=0 и 𝐹(1)=1</p>\n\n<pre><code class=\"language-scala\">def fibonacci(n: Int): Int = {\n  if n &lt;= 1 then\n    n // базовый случай: fib(0) = 0, fib(1) = 1\n  else \n    fibonacci(n - 1) + fibonacci(n - 2) // рекурсивный случай: fib(n) = fib(n - 1) + fib(n - 2)\n}</code></pre>\n\n<p>&nbsp;</p>\n\n<p>Более сложный пример множественной рекурсии:</p>\n\n<p><img alt=\"\" height=\"381\" name=\"Диаграмма без названия.drawio(11).png\" src=\"https://ucarecdn.com/77a7d7e7-aa3e-4bd3-a0b2-0d8076f8f708/\" width=\"996\"></p>\n\n<p>Вступая в мир множественной рекурсии мы уже двумя ногами попадаем в царство логики и математики, что требует усердия в осознании происходящего. Очень много возможностей и альтернатив. Любой алгоритм можно реализовать&nbsp; несколькими способами, и он может стать или супер наглядным или непостижимым для понимания. Поэтому множественная рекурсия является самым тонким и красивым способом построения вычислений, и является настоящей волшебной палочкой профессионалов.</p>\n\n<p>Пример <span style=\"background-color:#ffffff; color:#0d0d0d; font-size:16px; text-align:start;\">вычисления количества способов размена заданной суммы денег с использованием заданных монет.:</span></p>\n\n<pre><code class=\"language-scala\">// money - сумма, coins - список монет номиналом от 1 до 9 в строке \ndef countChange(money: Int, coins: String): Int = {\n  if money == 0 then \n    1 // Базовый случай: если сумма денег равна 0, то есть один способ размена - ничего не выбирать\n  else if money &lt; 0 || coins.isEmpty then\n    0 // Базовый случай: если сумма денег меньше 0 или монет нет, то способов размена нет\n  else \n    countChange(money, coins.tail) + countChange(money - coins.head.toString.toInt, coins) // Рекурсивный случай: вычисляем количество способов размена\n}</code></pre>\n\n<p>&nbsp;</p>\n\n<p>Более полная диаграмма множественной рекурсии:</p>\n\n<p><img alt=\"\" height=\"442\" name=\"Диаграмма без названия.drawio(12).png\" src=\"https://ucarecdn.com/234ec2a3-4eda-40a9-bd08-11b89cc4feaa/\" width=\"1056\"></p>",
  "video" : null,
  "options" : {
    
  },
  "subtitle_files" : [
  ],
  "is_deprecated" : false
}
```

</details>
