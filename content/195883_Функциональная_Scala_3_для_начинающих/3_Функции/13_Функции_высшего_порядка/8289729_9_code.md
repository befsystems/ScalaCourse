# Программирование (Шаг 9)

## Информация о шаге

- **ID шага**: 8289729
- **Позиция**: 9
- **Тип**: code
- **Курс**: Функциональная Scala 3 для начинающих
- **Экспортирован**: 2025-12-20T22:21:31.159447

## Содержание

### Создать функцию-генератор, которая создает функцию для операции **filter** строки.

### Сгенерированная функция должна обеспечивать следующую фильтрацию: если длина строки больше 3, то надо оставлять только числовые символы, если нет, то буквенные.

## Начальный код

**Язык:** scala3

```scala3
val filterGen: String => (Char => Boolean) = ??? // Реализация тут

val res = arg.filter(filterGen(arg))
```

**Ограничение по времени:** 5 сек

**Ограничение по памяти:** 256 МБ

---

### Полные данные JSON

<details>
<summary>Показать JSON данные</summary>

```json
{
  "name" : "code",
  "text" : "<h3>Создать функцию-генератор, которая создает функцию для операции <strong>filter</strong> строки.</h3>\n\n<h3>Сгенерированная функция должна обеспечивать следующую фильтрацию: если длина строки больше 3, то надо оставлять только числовые символы, если нет, то буквенные.</h3>",
  "video" : null,
  "options" : {
    "execution_time_limit" : 5,
    "execution_memory_limit" : 256,
    "limits" : {
      "scala3" : {
        "time" : 9,
        "memory" : 256
      }
    },
    "code_templates" : {
      "scala3" : "val filterGen: String => (Char => Boolean) = ??? // Реализация тут\n\nval res = arg.filter(filterGen(arg))"
    },
    "code_templates_header_lines_count" : {
      "scala3" : 3
    },
    "code_templates_footer_lines_count" : {
      "scala3" : 2
    },
    "code_templates_options" : {
      
    },
    "samples" : [
      [
        "abc",
        "abc"
      ]
    ],
    "is_run_user_code_allowed" : true
  },
  "subtitle_files" : [
  ],
  "is_deprecated" : false
}
```

</details>
