# Материал (Шаг 1)

## Информация о шаге

- **ID шага**: 6057692
- **Позиция**: 1
- **Тип**: text
- **Курс**: Функциональная Scala 3 для начинающих
- **Экспортирован**: 2026-01-10T10:17:20.811375

## Содержание

# Композиция функций

**Композиция функций** — это процесс **математического **объединения нескольких функций в одну. Она представляет собой  применения одной функции к результату другой функции.

Композируемые функции должны отвечать определенным требованиям.

## Математика композиции функций

Композиция может быть выполнена только для **одноместных** функций (унарных, функций с одним параметром). Но все функции методом **каррирования **могут быть представлены как одноместные.

Когда вы композируете функции, то результат одной функции передается в другую функцию как аргумент. Композиция функции в математике представляется как \(f \circ g\), что означает - *сначала выполните функцию g, затем выполните функцию f на результате функции g*.

Пример:

*Пусть у нас есть две функции:*

\(𝑓(𝑥) = 𝑥 + 1\)

\(g(x)=2 × x\)

*Тогда композиция \(f \circ g\) будет выглядеть так:*

\((f∘g)(x)=f(g(x))=f(2×x)=(2×x)+1\)

 

### Свойства композиции функций

**Не коммутативность**:  \(𝑓(𝑔(𝑥))≠𝑔(𝑓(𝑥))\)     (в общем случае).

**Ассоциативность**: Композиция трёх функций f, g и h ассоциативна, то есть \(f(g(h(x)))=(f∘g)∘h(x)=f∘(g∘h)(x)\)

**Тождественная функция:**   \(id(x)=x\)   не меняет результат при композиции с любой функцией.

**Композиция обратных функций: **Композиция функции и её обратной функции даёт тождественную функцию: \(f(f ^{−1} (x))=f^{−1} (f(x))=x\)

### Полезность композиции функций

Композиция позволяет создавать математически надежные цепочки преобразований данных, обеспечивая возможность лаконичного и выразительного программирования. Есть также гарантия выполнения всех свойств композиции функций, ими можно безбоязненно пользоваться.

В функциональном стиле композиция функций играет важную роль. Она позволяет создавать сложные функции на основе более простых, что улучшает читаемость и поддержку кода.

Идеология функционального стиля подразумевает построение всего кода программы из небольших, желательно чистых функций, которые посредством **композиции**** **объединяются в более крупные функции. В итоге получается одна функция, которая и запускается на выполнение.

---

### Полные данные JSON

<details>
<summary>Показать JSON данные</summary>

```json
{
  "name" : "text",
  "text" : "<h1 style=\"text-align:center;\">Композиция функций</h1>\n\n<p><strong>Композиция функций</strong> — это процесс <strong>математического </strong>объединения нескольких функций в одну. Она представляет собой  применения одной функции к результату другой функции.</p>\n\n<p>Композируемые функции должны отвечать определенным требованиям.</p>\n\n<h2 style=\"text-align:center;\">Математика композиции функций</h2>\n\n<p>Композиция может быть выполнена только для <strong>одноместных</strong> функций (унарных, функций с одним параметром). Но все функции методом <strong>каррирования </strong>могут быть представлены как одноместные.</p>\n\n<p>Когда вы композируете функции, то результат одной функции передается в другую функцию как аргумент. Композиция функции в математике представляется как <span class=\"math-tex\">\\(f \\circ g\\)</span>, что означает - <em>сначала выполните функцию g, затем выполните функцию f на результате функции g</em>.</p>\n\n<p>Пример:</p>\n\n<p><em>Пусть у нас есть две функции:</em></p>\n\n<p><span class=\"math-tex\">\\(𝑓(𝑥) = 𝑥 + 1\\)</span></p>\n\n<p><span class=\"math-tex\">\\(g(x)=2 × x\\)</span></p>\n\n<p><em>Тогда композиция <span class=\"math-tex\">\\(f \\circ g\\)</span> будет выглядеть так:</em><br>\n<span class=\"math-tex\">\\((f∘g)(x)=f(g(x))=f(2×x)=(2×x)+1\\)</span></p>\n\n<p> </p>\n\n<h3 style=\"text-align:center;\">Свойства композиции функций</h3>\n\n<p><strong>Не коммутативность</strong>:  <span class=\"math-tex\">\\(𝑓(𝑔(𝑥))≠𝑔(𝑓(𝑥))\\)</span>     (в общем случае).</p>\n\n<p><strong>Ассоциативность</strong>: Композиция трёх функций f, g и h ассоциативна, то есть <span class=\"math-tex\">\\(f(g(h(x)))=(f∘g)∘h(x)=f∘(g∘h)(x)\\)</span></p>\n\n<p><strong>Тождественная функция:</strong>   <span class=\"math-tex\">\\(id(x)=x\\)</span>   не меняет результат при композиции с любой функцией.</p>\n\n<p><strong>Композиция обратных функций: </strong>Композиция функции и её обратной функции даёт тождественную функцию: <span class=\"math-tex\">\\(f(f ^{−1} (x))=f^{−1} (f(x))=x\\)</span></p>\n\n<h3 style=\"text-align:center;\">Полезность композиции функций</h3>\n\n<p>Композиция позволяет создавать математически надежные цепочки преобразований данных, обеспечивая возможность лаконичного и выразительного программирования. Есть также гарантия выполнения всех свойств композиции функций, ими можно безбоязненно пользоваться.</p>\n\n<p>В функциональном стиле композиция функций играет важную роль. Она позволяет создавать сложные функции на основе более простых, что улучшает читаемость и поддержку кода.</p>\n\n<p>Идеология функционального стиля подразумевает построение всего кода программы из небольших, желательно чистых функций, которые посредством <strong>композиции</strong><strong> </strong>объединяются в более крупные функции. В итоге получается одна функция, которая и запускается на выполнение.</p>",
  "video" : null,
  "options" : {
    
  },
  "subtitle_files" : [
  ],
  "is_deprecated" : false
}
```

</details>
