# Программирование (Шаг 4)

## Информация о шаге

- **ID шага**: 5466332
- **Позиция**: 4
- **Тип**: code
- **Курс**: Функциональная Scala 3 для начинающих
- **Экспортирован**: 2025-12-20T22:21:11.3742825

## Содержание

**Проблема:** Вам постоянно нужно отправлять уведомления через одни и те же сервисы (Email, SMS, Push) с определенными приоритетами.

- Создайте каррированную версию функции sendNotification

Создайте специализированные функции для каждого сервиса:

- `emailNotifier` - для Email уведомлений

- `smsNotifier` - для SMS уведомлений

- `pushNotifier` - для Push уведомлений



Создайте функцию для критических уведомлений через Email:

- `emailCritical` - критические Email уведомления

## Начальный код

**Язык:** scala3

```scala3
// Есть уже готовая функция для формирования уведомлений    
def sendNotification(service: String, priority: String, message: String): String =
  s"[$service] $priority: $message"

// Доступные сервисы
val emailService = "Email"
val smsService = "SMS"
val pushService = "Push"

// Доступные приоритеты уведомлений
val criticalPriority = "CRITICAL"
val infoPriority = "INFO"

// Каррированная версия функции sendNotification
val curriedNotification: String => String => String => String = ???

// Создание специализированных функций
val emailNotifier: String => String => String = curriedNotification ???
val emailInfo: String => String = emailNotifier ???
val emailCritical: String => String = emailNotifier ???
val smsCritical: String => String = curriedNotification ???
val pushInfo: String => String = curriedNotification ???

// Теперь просто формируем нужные уведомления
val serverInfo = emailInfo("Server maintenance completed")
val serverError = emailCritical("Server maintenance error")
val urgentSms = smsCritical("Your account was locked")
val accountInfo = pushInfo("Account created")
```

**Ограничение по времени:** 5 сек

**Ограничение по памяти:** 256 МБ

---

### Полные данные JSON

<details>
<summary>Показать JSON данные</summary>

```json
{
  "name" : "code",
  "text" : "<p><strong>Проблема:</strong> Вам постоянно нужно отправлять уведомления через одни и те же сервисы (Email, SMS, Push) с определенными приоритетами.</p>\n\n<ol>\n\t<li>Создайте каррированную версию функции sendNotification</li>\n\t<li>Создайте специализированные функции для каждого сервиса:\n\t<ul>\n\t\t<li><code>emailNotifier</code> - для Email уведомлений</li>\n\t\t<li><code>smsNotifier</code> - для SMS уведомлений</li>\n\t\t<li><code>pushNotifier</code> - для Push уведомлений</li>\n\t</ul>\n\t</li>\n\t<li>Создайте функцию для критических уведомлений через Email:\n\t<ul>\n\t\t<li><code>emailCritical</code> - критические Email уведомления</li>\n\t</ul>\n\t</li>\n</ol>",
  "video" : null,
  "options" : {
    "execution_time_limit" : 5,
    "execution_memory_limit" : 256,
    "limits" : {
      "scala3" : {
        "time" : 9,
        "memory" : 256
      }
    },
    "code_templates" : {
      "scala3" : "// Есть уже готовая функция для формирования уведомлений    \ndef sendNotification(service: String, priority: String, message: String): String =\n  s\"[$service] $priority: $message\"\n\n// Доступные сервисы\nval emailService = \"Email\"\nval smsService = \"SMS\"\nval pushService = \"Push\"\n\n// Доступные приоритеты уведомлений\nval criticalPriority = \"CRITICAL\"\nval infoPriority = \"INFO\"\n\n// Каррированная версия функции sendNotification\nval curriedNotification: String => String => String => String = ???\n\n// Создание специализированных функций\nval emailNotifier: String => String => String = curriedNotification ???\nval emailInfo: String => String = emailNotifier ???\nval emailCritical: String => String = emailNotifier ???\nval smsCritical: String => String = curriedNotification ???\nval pushInfo: String => String = curriedNotification ???\n\n// Теперь просто формируем нужные уведомления\nval serverInfo = emailInfo(\"Server maintenance completed\")\nval serverError = emailCritical(\"Server maintenance error\")\nval urgentSms = smsCritical(\"Your account was locked\")\nval accountInfo = pushInfo(\"Account created\")"
    },
    "code_templates_header_lines_count" : {
      "scala3" : 1
    },
    "code_templates_footer_lines_count" : {
      "scala3" : 5
    },
    "code_templates_options" : {
      
    },
    "samples" : [
      [
        "",
        "[Email] INFO: Server maintenance completed\n[Email] CRITICAL: Server maintenance error\n[SMS] CRITICAL: Your account was locked\n[Push] INFO: Account created"
      ]
    ],
    "is_run_user_code_allowed" : true
  },
  "subtitle_files" : [
  ],
  "is_deprecated" : false
}
```

</details>
