# Материал (Шаг 3)

## Информация о шаге

- **ID шага**: 5084278
- **Позиция**: 3
- **Тип**: text
- **Курс**: Функциональная Scala 3 для начинающих
- **Экспортирован**: 2025-12-20T22:21:07.3843481

## Содержание

## Ошибка обработки

Ошибка при извлечении содержимого.

```json
{
  "name" : "text",
  "text" : "<h1 style=\"text-align:center;\">Выражения в операторной нотации</h1>\n\n<p><strong>Операторная нотация</strong> — это стиль записи выражений, в котором операции записываются в виде операторов (символов или их комбинаций), применяемых к операндам. Такой стиль широко используется для записи математических и логических операций. Операторная нотация стремится к компактности и удобству восприятия.</p>\n\n<p>Пример выражения в операторной нотации:</p>\n\n<pre><code class=\"language-scala\">1 + 2 + 3 max 4</code></pre>\n\n<h2 style=\"text-align:center;\">Виды операторной нотации</h2>\n\n<p>Операторная нотация подразделяется на три основные формы:</p>\n\n<ol>\n\t<li>\n\t<p><strong>Инфиксная нотация</strong><br>\n\tОператор располагается <strong>между операндами</strong>.</p>\n\n\t<p>Пример:</p>\n\n\t<pre><code class=\"language-scala\">1 + 2</code></pre>\n\n\t<p>Здесь <code>+</code> — оператор, <code>1</code> и <code>2</code> — операнды.</p>\n\t</li>\n\t<li>\n\t<p><strong>Префиксная нотация</strong><br>\n\tОператор располагается <strong>перед операндами</strong>.</p>\n\n\t<p>Пример:</p>\n\n\t<pre><code class=\"language-scala\">-5</code></pre>\n\n\t<p>Здесь <code>-</code> — оператор, который применяется к <code>5</code>.</p>\n\t</li>\n\t<li>\n\t<p><strong>Постфиксная нотация</strong><br>\n\tОператор располагается <strong>после операндов</strong>.</p>\n\n\t<p>Пример:</p>\n\n\t<pre><code class=\"language-scala\">5! </code></pre>\n\n\t<p>Здесь <code>!</code> — оператор факториала, применяемый к <code>5</code>. </p>\n\t</li>\n</ol>\n\n<p><strong>Внимание!</strong> Постфиксная нотация <em>запрещена в версии Scala 3</em> в связи с неоднозначностью восприятия сложных выражений с постфиксной нотацией. Соответственно, не всякое выражение можно теперь полностью записать в операторной нотации, необходимо комбинировать операторную нотацию с точечной нотацией, о которой позже.</p>\n\n<p> </p>\n\n<h2 style=\"text-align:center;\">Приоритеты операций в операторной нотации</h2>\n\n<p>Операторная нотация привносит и традиционные математические <strong>приоритеты операций</strong>. Эти приоритеты являются неявными, о них нужно помнить программистам.</p>\n\n<p>Пример:</p>\n\n<pre><code class=\"language-scala\">1 + 2 + 3 max 4 * 17</code></pre>\n\n<p>имеет результат 68</p>\n\n<p>Зрительно можно запутаться в расстановке приоритетов операций в выражении. Поэтому есть возможность указывать приоритеты <strong>явно с помощью круглых скобок</strong>. Пример:</p>\n\n<pre><code class=\"language-scala\">(1 + 2 + 3) max (4 * 17)</code></pre>\n\n<p>имеет результат 68</p>\n\n<p>Уже намного проще понять и зрительно вычислить выражение.</p>\n\n<p> </p>\n\n<p> </p>\n\n<p>Компилятор принимает <strong>решение о приоритете</strong> операций в операторной нотации на основе первого символа операции. Если имя операции начинается, к примеру, с <code>*</code>, то он получит более высокий приоритет, чем операция, чье имя начинается на <code>+</code>. Следовательно, выражение <code>2 + 2 * 7</code> будет вычислено как <code>2 + (2 * 7)</code>.</p>\n\n<p>Выражение <code>1 +++ 2 *** 3</code>, в котором <code>+++</code> и <code>***</code> есть некие операции, будет вычислено как <code>1 +++ (2 *** 3)</code>, поскольку операция <code>***</code> обладает более высоким уровнем приоритета, чем операция <code>+++</code>. Приоритет был определен по первым символам операций.</p>\n\n<p><strong>Порядок приоритета (от высокого к низкому):</strong></p>\n\n<ol>\n\t<li>Постфиксные операции (<code>obj.method</code>)</li>\n\t<li>Унарные операции (<code>-x</code>, <code>!x</code>)</li>\n\t<li>Мультипликативные (<code>*</code>, <code>/</code>, <code>%</code>)</li>\n\t<li>Аддитивные (<code>+</code>, <code>-</code>)</li>\n\t<li>Сравнения (<code>&lt;</code>, <code>&gt;</code>, <code>&lt;=</code>, <code>&gt;=</code>)</li>\n\t<li>Равенства (<code>==</code>, <code>!=</code>)</li>\n\t<li>Логическое И (<code>&amp;&amp;</code>)</li>\n\t<li>Логическое ИЛИ (<code>||</code>)</li>\n\t<li>Присваивание (<code>=</code>)</li>\n</ol>\n\n<p><strong>Таблица определения приоритетов операций по первым символов операторов:</strong></p>\n\n<table align=\"center\" border=\"1\" style=\"height:430px; width:433px;\">\n\t<tbody>\n\t\t<tr>\n\t\t\t<th>Первый символ операции</th>\n\t\t\t<th>Приоритет</th>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td style=\"text-align:center;\">префиксные операции: +, -, !, ~</td>\n\t\t\t<td style=\"text-align:center;\">высший</td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td style=\"text-align:center;\">* / %</td>\n\t\t\t<td style=\"text-align:center;\"> </td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td style=\"text-align:center;\">+ -</td>\n\t\t\t<td style=\"text-align:center;\"> </td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td style=\"text-align:center;\">:</td>\n\t\t\t<td> </td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td style=\"text-align:center;\">= !</td>\n\t\t\t<td> </td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td style=\"text-align:center;\">&lt; &gt;</td>\n\t\t\t<td> </td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td style=\"text-align:center;\">&amp;</td>\n\t\t\t<td> </td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td style=\"text-align:center;\">^</td>\n\t\t\t<td> </td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td style=\"text-align:center;\">буквы, $, _</td>\n\t\t\t<td style=\"text-align:center;\">низший</td>\n\t\t</tr>\n\t</tbody>\n</table>\n\n<p><strong>Высший </strong>приоритет имеют <strong>префиксные </strong>операции.</p>\n\n<p>Примеры операции с высшим приоритетом:</p>\n\n<pre><code class=\"language-scala\">-1\n~1</code></pre>\n\n<p>Операции начинающиеся с <strong>буквы </strong>имеют самый <strong>низший</strong> приоритет.</p>\n\n<p>Пример операции с низшем приоритетом:</p>\n\n<pre><code>3 max 4</code></pre>\n\n<p>Если приоритет операций одинаков, то операции выполняются <strong>слева направо</strong>.</p>\n\n<p>Круглые скобки меняют приоритеты операций.</p>\n\n<p>Операторная нотация часто используется для математических и логических выражений, как более привычная.</p>",
  "video" : null,
  "options" : {
    
  },
  "subtitle_files" : [
  ],
  "is_deprecated" : false
}
```

---

### Полные данные JSON

<details>
<summary>Показать JSON данные</summary>

```json
{
  "name" : "text",
  "text" : "<h1 style=\"text-align:center;\">Выражения в операторной нотации</h1>\n\n<p><strong>Операторная нотация</strong> — это стиль записи выражений, в котором операции записываются в виде операторов (символов или их комбинаций), применяемых к операндам. Такой стиль широко используется для записи математических и логических операций. Операторная нотация стремится к компактности и удобству восприятия.</p>\n\n<p>Пример выражения в операторной нотации:</p>\n\n<pre><code class=\"language-scala\">1 + 2 + 3 max 4</code></pre>\n\n<h2 style=\"text-align:center;\">Виды операторной нотации</h2>\n\n<p>Операторная нотация подразделяется на три основные формы:</p>\n\n<ol>\n\t<li>\n\t<p><strong>Инфиксная нотация</strong><br>\n\tОператор располагается <strong>между операндами</strong>.</p>\n\n\t<p>Пример:</p>\n\n\t<pre><code class=\"language-scala\">1 + 2</code></pre>\n\n\t<p>Здесь <code>+</code> — оператор, <code>1</code> и <code>2</code> — операнды.</p>\n\t</li>\n\t<li>\n\t<p><strong>Префиксная нотация</strong><br>\n\tОператор располагается <strong>перед операндами</strong>.</p>\n\n\t<p>Пример:</p>\n\n\t<pre><code class=\"language-scala\">-5</code></pre>\n\n\t<p>Здесь <code>-</code> — оператор, который применяется к <code>5</code>.</p>\n\t</li>\n\t<li>\n\t<p><strong>Постфиксная нотация</strong><br>\n\tОператор располагается <strong>после операндов</strong>.</p>\n\n\t<p>Пример:</p>\n\n\t<pre><code class=\"language-scala\">5! </code></pre>\n\n\t<p>Здесь <code>!</code> — оператор факториала, применяемый к <code>5</code>. </p>\n\t</li>\n</ol>\n\n<p><strong>Внимание!</strong> Постфиксная нотация <em>запрещена в версии Scala 3</em> в связи с неоднозначностью восприятия сложных выражений с постфиксной нотацией. Соответственно, не всякое выражение можно теперь полностью записать в операторной нотации, необходимо комбинировать операторную нотацию с точечной нотацией, о которой позже.</p>\n\n<p> </p>\n\n<h2 style=\"text-align:center;\">Приоритеты операций в операторной нотации</h2>\n\n<p>Операторная нотация привносит и традиционные математические <strong>приоритеты операций</strong>. Эти приоритеты являются неявными, о них нужно помнить программистам.</p>\n\n<p>Пример:</p>\n\n<pre><code class=\"language-scala\">1 + 2 + 3 max 4 * 17</code></pre>\n\n<p>имеет результат 68</p>\n\n<p>Зрительно можно запутаться в расстановке приоритетов операций в выражении. Поэтому есть возможность указывать приоритеты <strong>явно с помощью круглых скобок</strong>. Пример:</p>\n\n<pre><code class=\"language-scala\">(1 + 2 + 3) max (4 * 17)</code></pre>\n\n<p>имеет результат 68</p>\n\n<p>Уже намного проще понять и зрительно вычислить выражение.</p>\n\n<p> </p>\n\n<p> </p>\n\n<p>Компилятор принимает <strong>решение о приоритете</strong> операций в операторной нотации на основе первого символа операции. Если имя операции начинается, к примеру, с <code>*</code>, то он получит более высокий приоритет, чем операция, чье имя начинается на <code>+</code>. Следовательно, выражение <code>2 + 2 * 7</code> будет вычислено как <code>2 + (2 * 7)</code>.</p>\n\n<p>Выражение <code>1 +++ 2 *** 3</code>, в котором <code>+++</code> и <code>***</code> есть некие операции, будет вычислено как <code>1 +++ (2 *** 3)</code>, поскольку операция <code>***</code> обладает более высоким уровнем приоритета, чем операция <code>+++</code>. Приоритет был определен по первым символам операций.</p>\n\n<p><strong>Порядок приоритета (от высокого к низкому):</strong></p>\n\n<ol>\n\t<li>Постфиксные операции (<code>obj.method</code>)</li>\n\t<li>Унарные операции (<code>-x</code>, <code>!x</code>)</li>\n\t<li>Мультипликативные (<code>*</code>, <code>/</code>, <code>%</code>)</li>\n\t<li>Аддитивные (<code>+</code>, <code>-</code>)</li>\n\t<li>Сравнения (<code>&lt;</code>, <code>&gt;</code>, <code>&lt;=</code>, <code>&gt;=</code>)</li>\n\t<li>Равенства (<code>==</code>, <code>!=</code>)</li>\n\t<li>Логическое И (<code>&amp;&amp;</code>)</li>\n\t<li>Логическое ИЛИ (<code>||</code>)</li>\n\t<li>Присваивание (<code>=</code>)</li>\n</ol>\n\n<p><strong>Таблица определения приоритетов операций по первым символов операторов:</strong></p>\n\n<table align=\"center\" border=\"1\" style=\"height:430px; width:433px;\">\n\t<tbody>\n\t\t<tr>\n\t\t\t<th>Первый символ операции</th>\n\t\t\t<th>Приоритет</th>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td style=\"text-align:center;\">префиксные операции: +, -, !, ~</td>\n\t\t\t<td style=\"text-align:center;\">высший</td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td style=\"text-align:center;\">* / %</td>\n\t\t\t<td style=\"text-align:center;\"> </td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td style=\"text-align:center;\">+ -</td>\n\t\t\t<td style=\"text-align:center;\"> </td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td style=\"text-align:center;\">:</td>\n\t\t\t<td> </td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td style=\"text-align:center;\">= !</td>\n\t\t\t<td> </td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td style=\"text-align:center;\">&lt; &gt;</td>\n\t\t\t<td> </td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td style=\"text-align:center;\">&amp;</td>\n\t\t\t<td> </td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td style=\"text-align:center;\">^</td>\n\t\t\t<td> </td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td style=\"text-align:center;\">буквы, $, _</td>\n\t\t\t<td style=\"text-align:center;\">низший</td>\n\t\t</tr>\n\t</tbody>\n</table>\n\n<p><strong>Высший </strong>приоритет имеют <strong>префиксные </strong>операции.</p>\n\n<p>Примеры операции с высшим приоритетом:</p>\n\n<pre><code class=\"language-scala\">-1\n~1</code></pre>\n\n<p>Операции начинающиеся с <strong>буквы </strong>имеют самый <strong>низший</strong> приоритет.</p>\n\n<p>Пример операции с низшем приоритетом:</p>\n\n<pre><code>3 max 4</code></pre>\n\n<p>Если приоритет операций одинаков, то операции выполняются <strong>слева направо</strong>.</p>\n\n<p>Круглые скобки меняют приоритеты операций.</p>\n\n<p>Операторная нотация часто используется для математических и логических выражений, как более привычная.</p>",
  "video" : null,
  "options" : {
    
  },
  "subtitle_files" : [
  ],
  "is_deprecated" : false
}
```

</details>
