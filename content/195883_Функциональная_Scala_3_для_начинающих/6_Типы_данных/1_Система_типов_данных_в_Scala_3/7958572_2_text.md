# Материал (Шаг 2)

## Информация о шаге

- **ID шага**: 7958572
- **Позиция**: 2
- **Тип**: text
- **Курс**: Функциональная Scala 3 для начинающих
- **Экспортирован**: 2025-12-20T22:21:44.0140448

## Содержание

# Базовые типы данных в системе Scala 3

Эти типы данных являются **фундаментальными единицами** в системе типов Scala 3. На их основе строятся более сложные типы. 

## Примитивные типы

val byte: Byte = 127
val short: Short = 32767
val int: Int = 42
val long: Long = 42L
val float: Float = 3.14f
val double: Double = 3.14159
val boolean: Boolean = true
val char: Char = 'A'
val unit: Unit = () // Примитивный singleton type

## Одноэлементные типы (Singleton Types)

### Единичный тип (Unit Type)

**Unit** - это примитивный singleton type с единственным значением `()`, используемый для функций с побочными эффектами. Используется в математике системы типов. Логически означает **присутствие значения**. 

def sideEffect(): Unit = println("Hello")
val unitValue: Unit = ()

### Пустой тип (Null Type) 

**Null** - это singleton type с единственным значением `null`. Является подтипом всех reference types, но НЕ является подтипом value types. Логически означает **отсутствие значения**. 

val nullValue: Null = null
val nullString: String = null // Null <: String

### Объекты-одиночки (Singleton Objects)

**Singleton objects** представляют типы с единственным значением-объектом. Имя типа полностью совпадает с его значением. Является единственным представителем сущности со своим типом. Предназначен для создания сложных типов.

object One
object Two
object Three

### Литеральные типы (Literal Types)

**Literal types** представляют типы с единственным значением для литералов (элементов) примитивных типов или типа String. Имя типа полностью совпадает с его значением. Предназначен для создания сложных типов и более узкой типизации. 

val port: 8080 = 8080
val env: "production" = "production"

##  Специальные типы

### Тип самого верхнего уровня (Any Type)

**Any** - это корень иерархии всех типов Scala (Top Type), **супертип всех других типов** в системе.

val anything: Any = 42
val anything2: Any = "hello"
val anything3: Any = Tuple3(1, 2, 3)

### Тип верхнего уровня для объектных типов (AnyRef  Type)

**AnyRef** - это **супертип всех объектных типов** (Reference Top Type), эквивалент java.lang.Object.

val ref1: AnyRef = "string"
val ref2: AnyRef = Tuple3(1, 2, 3)

// AnyRef не включает value types
// val ref4: AnyRef = 42 // Ошибка компиляции!

### Тип верхнего уровня для примитивных типов (AnyVal Type)

**AnyVal** - это **супертип всех примитивных типов** (Value Top Type), включая пользовательские **value classes**.

val value1: AnyVal = 42
val value2: AnyVal = 3.14
val value3: AnyVal = true

### Тип отсутствия возможностей (Nothing Type)

**Nothing** - это **крайне нижний тип** (Bottom Type) в иерархии типов Scala, **подтип всех других типов**. Это тип** **означает **отсутствие возможностей** - невозможно создать значение типа Nothing, невозможно нормально вернуться из функции с таким типом. Используется в математике системы типов.

У типа **нет** реальных **значений**.

`def crash(): Nothing = throw new Exception() // Невозможно нормально вернуться `

##  Иерархия типов в Scala

// Упрощённая иерархия системы типов
// Any
// / \
// AnyVal AnyRef
// / | | \
// Unit Int String Object
// \ | \ /
// \ \ Null
// \ \ /
// Nothing

---

### Полные данные JSON

<details>
<summary>Показать JSON данные</summary>

```json
{
  "name" : "text",
  "text" : "<h1 style=\"text-align:center;\">Базовые типы данных в системе Scala 3</h1>\n\n<p>Эти типы данных являются <strong>фундаментальными единицами</strong> в системе типов Scala 3. На их основе строятся более сложные типы. </p>\n\n<h2 style=\"text-align:center;\">Примитивные типы</h2>\n\n<pre><code class=\"language-scala\">val byte: Byte = 127\nval short: Short = 32767\nval int: Int = 42\nval long: Long = 42L\nval float: Float = 3.14f\nval double: Double = 3.14159\nval boolean: Boolean = true\nval char: Char = 'A'\nval unit: Unit = ()  // Примитивный singleton type</code></pre>\n\n<h2 style=\"text-align:center;\">Одноэлементные типы (Singleton Types)</h2>\n\n<h3 style=\"text-align:center;\">Единичный тип (Unit Type)</h3>\n\n<p><strong>Unit</strong> - это примитивный singleton type с единственным значением <code>()</code>, используемый для функций с побочными эффектами. Используется в математике системы типов. Логически означает <strong>присутствие значения</strong>. </p>\n\n<pre><code class=\"language-scala\">def sideEffect(): Unit = println(\"Hello\")\nval unitValue: Unit = ()</code></pre>\n\n<h3 style=\"text-align:center;\">Пустой тип (Null Type) </h3>\n\n<p><strong>Null</strong> - это singleton type с единственным значением <code>null</code>. Является подтипом всех reference types, но НЕ является подтипом value types. Логически означает <strong>отсутствие значения</strong>. </p>\n\n<pre><code class=\"language-scala\">val nullValue: Null = null\nval nullString: String = null  // Null &lt;: String</code></pre>\n\n<h3 style=\"text-align:center;\">Объекты-одиночки (Singleton Objects)</h3>\n\n<p><strong>Singleton objects</strong> представляют типы с единственным значением-объектом. Имя типа полностью совпадает с его значением. Является единственным представителем сущности со своим типом. Предназначен для создания сложных типов.</p>\n\n<pre><code class=\"language-scala\">object One\nobject Two\nobject Three</code></pre>\n\n<h3 style=\"text-align:center;\">Литеральные типы (Literal Types)</h3>\n\n<p><strong>Literal types</strong> представляют типы с единственным значением для литералов (элементов) примитивных типов или типа String. Имя типа полностью совпадает с его значением. Предназначен для создания сложных типов и более узкой типизации. </p>\n\n<pre><code class=\"language-scala\">val port: 8080 = 8080\nval env: \"production\" = \"production\"</code></pre>\n\n<h2 style=\"text-align:center;\"> Специальные типы</h2>\n\n<h3 style=\"text-align:center;\">Тип самого верхнего уровня (Any Type)</h3>\n\n<p><strong>Any</strong> - это корень иерархии всех типов Scala (Top Type), <strong>супертип всех других типов</strong> в системе.</p>\n\n<pre><code class=\"language-scala\">val anything: Any = 42\nval anything2: Any = \"hello\"\nval anything3: Any = Tuple3(1, 2, 3)</code></pre>\n\n<h3 style=\"text-align:center;\">Тип верхнего уровня для объектных типов (AnyRef  Type)</h3>\n\n<p><strong>AnyRef</strong> - это <strong>супертип всех объектных типов</strong> (Reference Top Type), эквивалент java.lang.Object.</p>\n\n<pre><code class=\"language-scala\">val ref1: AnyRef = \"string\"\nval ref2: AnyRef = Tuple3(1, 2, 3)\n\n// AnyRef не включает value types\n// val ref4: AnyRef = 42  // Ошибка компиляции!</code></pre>\n\n<h3 style=\"text-align:center;\">Тип верхнего уровня для примитивных типов (AnyVal Type)</h3>\n\n<p><strong>AnyVal</strong> - это <strong>супертип всех примитивных типов</strong> (Value Top Type), включая пользовательские <strong>value classes</strong>.</p>\n\n<pre><code class=\"language-scala\">val value1: AnyVal = 42\nval value2: AnyVal = 3.14\nval value3: AnyVal = true</code></pre>\n\n<h3 style=\"text-align:center;\">Тип отсутствия возможностей (Nothing Type)</h3>\n\n<p><strong>Nothing</strong> - это <strong>крайне нижний тип</strong> (Bottom Type) в иерархии типов Scala, <strong>подтип всех других типов</strong>. Это тип<strong> </strong>означает <strong>отсутствие возможностей</strong> - невозможно создать значение типа Nothing, невозможно нормально вернуться из функции с таким типом. Используется в математике системы типов.</p>\n\n<p>У типа <strong>нет</strong> реальных <strong>значений</strong>.</p>\n\n<pre><code class=\"language-scala\">def crash(): Nothing = throw new Exception() // Невозможно нормально вернуться      </code></pre>\n\n<h2 style=\"text-align:center;\"> Иерархия типов в Scala</h2>\n\n<pre><code class=\"language-scala\">// Упрощённая иерархия системы типов\n//             Any\n//            /   \\\n//       AnyVal  AnyRef\n//       /   |      |   \\\n//    Unit  Int   String Object\n//      \\    |       \\   /\n//        \\   \\      Null\n//          \\  \\     /\n//            Nothing\n</code></pre>",
  "video" : null,
  "options" : {
    
  },
  "subtitle_files" : [
  ],
  "is_deprecated" : false
}
```

</details>
