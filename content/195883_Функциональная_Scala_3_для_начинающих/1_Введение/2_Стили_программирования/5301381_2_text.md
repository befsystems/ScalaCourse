# Материал (Шаг 2)

## Информация о шаге

- **ID шага**: 5301381
- **Позиция**: 2
- **Тип**: text
- **Курс**: Функциональная Scala 3 для начинающих
- **Экспортирован**: 2026-01-10T10:16:56.6914425

## Содержание

# Императивный стиль

**Императивный стиль** описывает процесс вычислений в виде инструкций, изменяющих состояния программы.

Основными понятиями стиля являются: **переменная** (ячейка памяти), **инструкция** (команда или оператор), **состояние**.

**Переменная** – поименованное место памяти, куда можно записать значение. Значение переменной можно читать столько раз, сколько пожелаете. Его можно стереть и на его место записать новое значение. Таким образом, переменные обладают значениями, которые могут изменяться в процессе исполнения программы.

**Инструкция** определяет некоторое действие, например присваивание, ввод, вывод, ветвление, цикл и др.

**Состояние** – значения некоторого множества (набора) переменных. Работа программы основана на изменении состояний (значений переменных). Входные данные программы представляют собой первоначальное состояние.



Императивное программирование учитывает принципы организации архитектуры компьютера, предложенную Джоном фон Нейманом, которая преобладает в настоящее время. Поэтому императивный стиль считается наиболее родным для компьютеров. Автомат, последовательно изменяющий свои состояния под управлением внешних воздействий, наиболее просто реализуется технически.

Императивное программирование наиболее выгодно для реализации небольших задач, где очень важна скорость исполнения на современных компьютерах.

Обычно **императивный стиль** противопоставляют **функциональному стилю**, характеристики которых противоположны.

---

### Полные данные JSON

<details>
<summary>Показать JSON данные</summary>

```json
{
  "name" : "text",
  "text" : "<h1 style=\"text-align:center;\">Императивный стиль</h1>\n\n<p><strong>Императивный стиль</strong> описывает процесс вычислений в виде инструкций, изменяющих состояния программы.</p>\n\n<p>Основными понятиями стиля являются: <strong>переменная</strong> (ячейка памяти), <strong>инструкция</strong> (команда или оператор), <strong>состояние</strong>.</p>\n\n<p><strong>Переменная</strong> – поименованное место памяти, куда можно записать значение. Значение переменной можно читать столько раз, сколько пожелаете. Его можно стереть и на его место записать новое значение. Таким образом, переменные обладают значениями, которые могут изменяться в процессе исполнения программы.</p>\n\n<p><strong>Инструкция</strong> определяет некоторое действие, например присваивание, ввод, вывод, ветвление, цикл и др.</p>\n\n<p><strong>Состояние</strong> – значения некоторого множества (набора) переменных. Работа программы основана на изменении состояний (значений переменных). Входные данные программы представляют собой первоначальное состояние.</p>\n\n<p>&nbsp;</p>\n\n<p>Императивное программирование учитывает принципы организации архитектуры компьютера, предложенную Джоном фон Нейманом, которая преобладает в настоящее время. Поэтому императивный стиль считается наиболее родным для компьютеров. Автомат, последовательно изменяющий свои состояния под управлением внешних воздействий, наиболее просто реализуется технически.</p>\n\n<p>Императивное программирование наиболее выгодно для реализации небольших задач, где очень важна скорость исполнения на современных компьютерах.</p>\n\n<p>Обычно <strong>императивный стиль</strong> противопоставляют <strong>функциональному стилю</strong>, характеристики которых противоположны.<br style=\"font-variant:normal; text-align:-webkit-auto;\">\n&nbsp;</p>\n\n<p><br style=\"font-variant:normal; text-align:-webkit-auto;\">\n&nbsp;</p>",
  "video" : null,
  "options" : {
    
  },
  "subtitle_files" : [
  ],
  "is_deprecated" : false
}
```

</details>
