# Программирование (Шаг 6)

## Информация о шаге

- **ID шага**: 9162324
- **Позиция**: 6
- **Тип**: code
- **Курс**:  Система типов Scala 3
- **Экспортирован**: 2026-01-10T10:36:27.5720433

## Содержание

### **Создайте тип **enum Operation**, как перечень возможных арифметических операций. Определите функцию **apply**, которая принимает операции типа **Operation **в качестве аргумента и выполняет их.**

## Начальный код

**Язык:** scala3

```scala3
enum Operation // Определите enum
// Операции: Add, Subtract, Multiply, Divide

def apply(op: Operation, a: Int, b: Int): Int = ???

val addRes = apply(???, arg1, arg2) 
val subtractRes = apply(???, arg1, arg2)
val multiplyRes = apply(???, arg1, arg2)
val divideRes = apply(???, arg1, arg2)
```

**Ограничение по времени:** 5 сек

**Ограничение по памяти:** 256 МБ

---

### Полные данные JSON

<details>
<summary>Показать JSON данные</summary>

```json
{
  "name" : "code",
  "text" : "<h3><strong>Создайте тип </strong><span style=\"color:#ff4363;\">enum Operation</span><strong>, как перечень возможных арифметических операций. Определите функцию </strong><span style=\"color:#ff4363;\">apply</span><strong>, которая принимает операции типа </strong><span style=\"color:#ff4363;\">Operation </span><strong>в качестве аргумента и выполняет их.</strong></h3>",
  "video" : null,
  "options" : {
    "execution_time_limit" : 5,
    "execution_memory_limit" : 256,
    "limits" : {
      "scala3" : {
        "time" : 9,
        "memory" : 256
      }
    },
    "code_templates" : {
      "scala3" : "enum Operation // Определите enum\n// Операции: Add, Subtract, Multiply, Divide\n\ndef apply(op: Operation, a: Int, b: Int): Int = ???\n\nval addRes = apply(???, arg1, arg2) \nval subtractRes = apply(???, arg1, arg2)\nval multiplyRes = apply(???, arg1, arg2)\nval divideRes = apply(???, arg1, arg2)"
    },
    "code_templates_header_lines_count" : {
      "scala3" : 4
    },
    "code_templates_footer_lines_count" : {
      "scala3" : 2
    },
    "code_templates_options" : {
      
    },
    "samples" : [
      [
        "2 2\n",
        "4 0 4 1\n"
      ]
    ],
    "is_run_user_code_allowed" : true
  },
  "subtitle_files" : [
  ],
  "is_deprecated" : false
}
```

</details>
