# Программирование (Шаг 8)

## Информация о шаге

- **ID шага**: 9232102
- **Позиция**: 8
- **Тип**: code
- **Курс**:  Система типов Scala 3
- **Экспортирован**: 2026-01-10T10:36:27.5750194

## Содержание

### Создайте enum Length для работы с длинами в разных единицах измерения. Реализуйте конверсии между длинами и отдельную функцию `compare` для сравнения длин. Обратите внимание на проблему потери точности.

## Начальный код

**Язык:** scala3

```scala3
// Реализуйте Length
// Meters, Kilometers, Centimeters     

/**
   * Сравнение длин 
   * Возвращает:
   *  -1, если a < b
   *  0, если a == b
   *  1, если a > b
   */    
def compare(a: Length, b: Length): Int = ???    


// Пример использования Length
val meterLength = Length.Meters(99)  
val kilometerLength = meterLength.toKilometers
val centimeterLength = meterLength.toCentimeters
val centimeterLength2 = Length.Centimeters(99)
val comparisonRes = compare(kilometerLength, centimeterLength2)  //  1
```

**Ограничение по времени:** 5 сек

**Ограничение по памяти:** 256 МБ

---

### Полные данные JSON

<details>
<summary>Показать JSON данные</summary>

```json
{
  "name" : "code",
  "text" : "<h3>Создайте <span style=\"color:#ff4363;\">enum Length</span> для работы с длинами в разных единицах измерения. Реализуйте <span style=\"color:#ff4363;\">конверсии</span> между длинами и отдельную функцию <span style=\"color:#ff4363;\"><code>compare</code></span> для сравнения длин. Обратите внимание на проблему потери точности.</h3>",
  "video" : null,
  "options" : {
    "execution_time_limit" : 5,
    "execution_memory_limit" : 256,
    "limits" : {
      "scala3" : {
        "time" : 9,
        "memory" : 256
      }
    },
    "code_templates" : {
      "scala3" : "// Реализуйте Length\n// Meters, Kilometers, Centimeters     \n\n/**\n   * Сравнение длин \n   * Возвращает:\n   *  -1, если a < b\n   *  0, если a == b\n   *  1, если a > b\n   */    \ndef compare(a: Length, b: Length): Int = ???    \n\n\n// Пример использования Length\nval meterLength = Length.Meters(99)  \nval kilometerLength = meterLength.toKilometers\nval centimeterLength = meterLength.toCentimeters\nval centimeterLength2 = Length.Centimeters(99)\nval comparisonRes = compare(kilometerLength, centimeterLength2)  //  1"
    },
    "code_templates_header_lines_count" : {
      "scala3" : 1
    },
    "code_templates_footer_lines_count" : {
      "scala3" : 19
    },
    "code_templates_options" : {
      
    },
    "samples" : [
      [
        "Meters(99) Centimeters(99)\n",
        "1\n"
      ]
    ],
    "is_run_user_code_allowed" : true
  },
  "subtitle_files" : [
  ],
  "is_deprecated" : false
}
```

</details>
