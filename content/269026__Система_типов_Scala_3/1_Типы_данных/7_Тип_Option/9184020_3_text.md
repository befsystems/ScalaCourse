# Материал (Шаг 3)

## Информация о шаге

- **ID шага**: 9184020
- **Позиция**: 3
- **Тип**: text
- **Курс**:  Система типов Scala 3
- **Экспортирован**: 2026-01-10T10:36:34.6114047

## Содержание

# Операции Option

## Операции извлечения значений

| Операция | Описание | Пример |
| --- | --- | --- |
| `get` | Извлекает значение (небезопасно!) | `Some(5).get // 5` `None.get // ошибка NoSuchElementException` |
| `getOrElse(default)` | Возвращает значение или default | `None.getOrElse(0) // 0` |
| `orElse(alternative)` | Возвращает Option или альтернативу | `None.orElse(Some(10)) // Some(10)` |
| `orNull` | Возвращает значение или null | `None.orNull // null` |

## Операции проверки

| Операция | Описание | Пример |
| --- | --- | --- |
| `isDefined` | Проверяет наличие значения | `Some(5).isDefined // true` |
| `isEmpty` | Проверяет отсутствие значения | `None.isEmpty // true` |
| `nonEmpty` | Противоположность isEmpty | `Some(5).nonEmpty // true` |
| `contains(value)` | Проверяет наличие конкретного значения | `Some(5).contains(5) // true` |
| `exists(predicate)` | Проверяет условие для значения | `Some(5).exists(_ > 3) // true` |
| `forall(predicate)` | Проверяет условие (true для None) | `None.forall(_ > 3) // true` |

## Операции трансформации

| Операция | Описание | Пример |
| --- | --- | --- |
| `map(f)` | Применяет функцию к значению | `Some(5).map(_ * 2) // Some(10)` |
| `flatMap(f)` | Применяет функцию, возвращающую Option | `Some(5).flatMap(x => Some(x * 2)) ` `// Some(10)` |
| `flatten` | Разворачивает вложенный Option | `Some(Some(5)).flatten // Some(5)` |
| `filter(predicate)` | Фильтрует по условию | `Some(5).filter(_ > 3) // Some(5)` |
| `filterNot(predicate)` | Инверсия filter | `Some(5).filterNot(_ > 3) // None` |
| `collect(partialFunction)` | Применяет частичную функцию | `Some(5).collect { ` `  case x if x > 3 => x * 2 ` `} // Some(10)` |

## Операции с побочными эффектами

| Операция | Описание | Пример |
| --- | --- | --- |
| `foreach(f)` | Выполняет действие для значения | `Some(5).foreach(println) // печатает 5` |
| `fold(ifEmpty)(f)` | Применяет функцию или возвращает default | `Some(5).fold(0)(_ * 2) // 10` |

## Операции преобразования типов

| Операция | Описание | Пример |
| --- | --- | --- |
| `toRight(left)` | Преобразует в Either | `Some(5).toRight("error") // Right(5)` |
| `toLeft(right)` | Преобразует в Either | `Some(5).toLeft("default") // Left(5)` |
| `iterator` | Создаёт итератор | `Some(5).iterator.toSeq // Seq(5)` |

---

### Полные данные JSON

<details>
<summary>Показать JSON данные</summary>

```json
{
  "name" : "text",
  "text" : "<h1 style=\"text-align:center;\">Операции Option</h1>\n\n<h2 style=\"text-align:center;\">Операции извлечения значений</h2>\n\n<table>\n\t<thead>\n\t\t<tr>\n\t\t\t<th>Операция</th>\n\t\t\t<th>Описание</th>\n\t\t\t<th>Пример</th>\n\t\t</tr>\n\t</thead>\n\t<tbody>\n\t\t<tr>\n\t\t\t<td><code>get</code></td>\n\t\t\t<td>Извлекает значение (небезопасно!)</td>\n\t\t\t<td>\n\t\t\t<p><code>Some(5).get // 5</code></p>\n\n\t\t\t<p><code>None.get // ошибка <span style=\"color:#000000;\">NoSuchElementException</span></code></p>\n\t\t\t</td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td><code>getOrElse(default)</code></td>\n\t\t\t<td>Возвращает значение или default</td>\n\t\t\t<td><code>None.getOrElse(0) // 0</code></td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td><code>orElse(alternative)</code></td>\n\t\t\t<td>Возвращает Option или альтернативу</td>\n\t\t\t<td><code>None.orElse(Some(10)) // Some(10)</code></td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td><code>orNull</code></td>\n\t\t\t<td>Возвращает значение или null</td>\n\t\t\t<td><code>None.orNull // null</code></td>\n\t\t</tr>\n\t</tbody>\n</table>\n\n<h2 style=\"text-align:center;\">Операции проверки</h2>\n\n<table>\n\t<thead>\n\t\t<tr>\n\t\t\t<th>Операция</th>\n\t\t\t<th>Описание</th>\n\t\t\t<th>Пример</th>\n\t\t</tr>\n\t</thead>\n\t<tbody>\n\t\t<tr>\n\t\t\t<td><code>isDefined</code></td>\n\t\t\t<td>Проверяет наличие значения</td>\n\t\t\t<td><code>Some(5).isDefined // true</code></td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td><code>isEmpty</code></td>\n\t\t\t<td>Проверяет отсутствие значения</td>\n\t\t\t<td><code>None.isEmpty // true</code></td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td><code>nonEmpty</code></td>\n\t\t\t<td>Противоположность isEmpty</td>\n\t\t\t<td><code>Some(5).nonEmpty // true</code></td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td><code>contains(value)</code></td>\n\t\t\t<td>Проверяет наличие конкретного значения</td>\n\t\t\t<td><code>Some(5).contains(5) // true</code></td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td><code>exists(predicate)</code></td>\n\t\t\t<td>Проверяет условие для значения</td>\n\t\t\t<td><code>Some(5).exists(_ &gt; 3) // true</code></td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td><code>forall(predicate)</code></td>\n\t\t\t<td>Проверяет условие (true для None)</td>\n\t\t\t<td><code>None.forall(_ &gt; 3) // true</code></td>\n\t\t</tr>\n\t</tbody>\n</table>\n\n<h2 style=\"text-align:center;\">Операции трансформации</h2>\n\n<table style=\"height:486px; width:963px;\">\n\t<thead>\n\t\t<tr>\n\t\t\t<th>Операция</th>\n\t\t\t<th>Описание</th>\n\t\t\t<th>Пример</th>\n\t\t</tr>\n\t</thead>\n\t<tbody>\n\t\t<tr>\n\t\t\t<td><code>map(f)</code></td>\n\t\t\t<td>Применяет функцию к значению</td>\n\t\t\t<td><code>Some(5).map(_ * 2) // Some(10)</code></td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td><code>flatMap(f)</code></td>\n\t\t\t<td>Применяет функцию, возвращающую Option</td>\n\t\t\t<td>\n\t\t\t<p><code>Some(5).flatMap(x =&gt; Some(x * 2)) </code></p>\n\n\t\t\t<p><code>// Some(10)</code></p>\n\t\t\t</td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td><code>flatten</code></td>\n\t\t\t<td>Разворачивает вложенный Option</td>\n\t\t\t<td><code>Some(Some(5)).flatten // Some(5)</code></td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td><code>filter(predicate)</code></td>\n\t\t\t<td>Фильтрует по условию</td>\n\t\t\t<td><code>Some(5).filter(_ &gt; 3) // Some(5)</code></td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td><code>filterNot(predicate)</code></td>\n\t\t\t<td>Инверсия filter</td>\n\t\t\t<td><code>Some(5).filterNot(_ &gt; 3) // None</code></td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td><code>collect(partialFunction)</code></td>\n\t\t\t<td>Применяет частичную функцию</td>\n\t\t\t<td>\n\t\t\t<p><code>Some(5).collect { </code></p>\n\n\t\t\t<p><code>  case x if x &gt; 3 =&gt; x * 2 </code></p>\n\n\t\t\t<p><code>} // Some(10)</code></p>\n\t\t\t</td>\n\t\t</tr>\n\t</tbody>\n</table>\n\n<h2 style=\"text-align:center;\">Операции с побочными эффектами</h2>\n\n<table>\n\t<thead>\n\t\t<tr>\n\t\t\t<th>Операция</th>\n\t\t\t<th>Описание</th>\n\t\t\t<th>Пример</th>\n\t\t</tr>\n\t</thead>\n\t<tbody>\n\t\t<tr>\n\t\t\t<td><code>foreach(f)</code></td>\n\t\t\t<td>Выполняет действие для значения</td>\n\t\t\t<td><code>Some(5).foreach(println) // печатает 5</code></td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td><code>fold(ifEmpty)(f)</code></td>\n\t\t\t<td>Применяет функцию или возвращает default</td>\n\t\t\t<td><code>Some(5).fold(0)(_ * 2) // 10</code></td>\n\t\t</tr>\n\t</tbody>\n</table>\n\n<h2 style=\"text-align:center;\">Операции преобразования типов</h2>\n\n<table>\n\t<thead>\n\t\t<tr>\n\t\t\t<th>Операция</th>\n\t\t\t<th>Описание</th>\n\t\t\t<th>Пример</th>\n\t\t</tr>\n\t</thead>\n\t<tbody>\n\t\t<tr>\n\t\t\t<td><code>toRight(left)</code></td>\n\t\t\t<td>Преобразует в Either</td>\n\t\t\t<td><code>Some(5).toRight(\"error\") // Right(5)</code></td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td><code>toLeft(right)</code></td>\n\t\t\t<td>Преобразует в Either</td>\n\t\t\t<td><code>Some(5).toLeft(\"default\") // Left(5)</code></td>\n\t\t</tr>\n\t\t<tr>\n\t\t\t<td><code>iterator</code></td>\n\t\t\t<td>Создаёт итератор</td>\n\t\t\t<td><code>Some(5).iterator.toSeq // Seq(5)</code></td>\n\t\t</tr>\n\t</tbody>\n</table>",
  "video" : null,
  "options" : {
    
  },
  "subtitle_files" : [
  ],
  "is_deprecated" : false
}
```

</details>
