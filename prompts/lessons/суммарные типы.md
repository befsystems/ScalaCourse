# Промт для создания статьи "Чистые суммарные типы в Scala 3"

Ты технический писатель, специализирующийся на функциональном программировании и системах типов. Напиши подробную обучающую статью о чистых суммарных типах в Scala 3.

## Целевая аудитория
- Scala-разработчики среднего уровня, знакомые с основами языка
- Читатели уже знают о произведениях типов из предыдущей статьи
- Понимают базовый синтаксис Scala 3 (case class, pattern matching)
- Хотят понять алгебраическую природу типов

## Структура статьи

### 1. Введение (2-3 абзаца)
- Что такое суммарный тип на интуитивном уровне ("либо это, либо то")
- Почему называется "суммарным" (алгебраическая аналогия)
- Отличие от произведений (ссылка на предыдущую статью): "И" vs "ИЛИ"
- Анонс: в этой статье рассматриваем только "чистые" суммы без данных, ADT с данными — в следующей статье

### 2. Простейший пример: Boolean как сумма (3-4 абзаца)
```scala
// Boolean = true | false
// Алгебраически: Boolean = 1 + 1 = 2
```
- Boolean — это простейший суммарный тип из двух вариантов
- Кардинальность: сколько возможных значений
- Связь с алгеброй: почему "сумма", а не "произведение"
- Сравнение с произведением (Boolean, Boolean) = 4 значения

### 3. Enum в Scala 3: синтаксис и базовое использование

#### 3.1 Определение простого enum
```scala
enum Color:
  case Red
  case Green
  case Blue
```
- Синтаксис Scala 3
- Как создавать значения: Color.Red
- Pattern matching
- Exhaustiveness checking (компилятор проверяет полноту)

#### 3.2 Алгебраическая интерпретация
```scala
// Color = Red + Green + Blue
// Color = 1 + 1 + 1 = 3
```
- Почему кардинальность = количество case-ов
- Каждый case без параметров = единица (1)
- Формула для подсчёта: n case-ов = n значений

### 4. Сравнение с другими подходами

#### 4.1 Scala 2: sealed trait
```scala
// Старый способ
sealed trait Color
object Color:
  case object Red extends Color
  case object Green extends Color
  case object Blue extends Color
```
- Более многословно
- Та же семантика
- Enum в Scala 3 — синтаксический сахар

#### 4.2 Java enum
```java
public enum Color {
    RED, GREEN, BLUE
}
```
- Похожий синтаксис
- Но Java enum — это классы с дополнительными возможностями
- Scala enum проще и математичнее

#### 4.3 TypeScript union of literals
```typescript
type Color = "red" | "green" | "blue"
```
- Это НЕ то же самое
- String literals vs отдельные типы
- В runtime это просто строки
- Union type ≠ Sum type (будет в отдельной статье)

### 5. Практические примеры

#### 5.1 Состояния конечного автомата
```scala
enum ConnectionState:
  case Disconnected
  case Connecting
  case Connected
  case Error

def handle(state: ConnectionState): String = state match
  case ConnectionState.Disconnected => "Нет соединения"
  case ConnectionState.Connecting => "Подключение..."
  case ConnectionState.Connected => "Подключено"
  case ConnectionState.Error => "Ошибка"
```

#### 5.2 Направления
```scala
enum Direction:
  case North, South, East, West

def opposite(d: Direction): Direction = d match
  case Direction.North => Direction.South
  case Direction.South => Direction.North
  case Direction.East => Direction.West
  case Direction.West => Direction.East
```

#### 5.3 HTTP методы
```scala
enum HttpMethod:
  case GET, POST, PUT, DELETE, PATCH
```

#### 5.4 Игровые состояния
```scala
enum GameState:
  case MainMenu
  case Playing
  case Paused
  case GameOver
```

### 6. Exhaustiveness checking — главное преимущество
```scala
enum Status:
  case Active
  case Inactive
  case Pending

def handle(s: Status): String = s match
  case Status.Active => "работает"
  case Status.Inactive => "остановлено"
  // Компилятор выдаст warning: match may not be exhaustive

// Добавляем новый case:
enum Status:
  case Active
  case Inactive
  case Pending
  case Archived  // новый!

// Все существующие match сломаются на компиляции!
```

- Type safety: невозможно забыть случай
- Рефакторинг: добавление нового case ломает компиляцию везде, где нужно обновить код
- Сравнение с magic strings или константами

### 7. Когда использовать чистые суммы

✅ **Используйте enum без параметров когда:**
- Фиксированное конечное множество вариантов
- Варианты не несут дополнительных данных
- Нужна проверка полноты на компиляции
- Моделируете состояния, категории, флаги

❌ **НЕ используйте когда:**
- Нужно хранить данные с каждым вариантом → ADT (следующая статья)
- Варианты могут динамически добавляться → String или Int
- Нужна расширяемость вне вашего кода → open hierarchy

### 8. Алгебра типов: формальный взгляд

#### 8.1 Кардинальность
```scala
enum Color:
  case Red, Green, Blue
// |Color| = 1 + 1 + 1 = 3

enum Bool:
  case True, False
// |Bool| = 1 + 1 = 2

// Композиция
type ColorAndBool = (Color, Bool)
// |ColorAndBool| = |Color| × |Bool| = 3 × 2 = 6
```

#### 8.2 Почему "сумма"
- Количество возможных значений складывается
- Vs произведение: количество значений умножается
- Математическая аналогия с числами

#### 8.3 Изоморфизмы
```scala
// Эти типы изоморфны (одинаковая кардинальность)
enum Binary1:
  case Zero, One

enum Binary2:
  case False, True

// Оба = 1 + 1 = 2
```

### 9. Отличие от union types (краткое упоминание)
```scala
// Чистая сумма (enum)
enum Color:
  case Red, Green, Blue
// |Color| = 3

// Union type
type ColorString = "red" | "green" | "blue"
// |ColorString| = 3, но это String в runtime

// Ключевая разница:
// - enum создаёт новые типы
// - union — ограничивает существующие типы
// - enum различимы в pattern matching по конструктору
// - union различимы по значению
```

Детальное сравнение будет в отдельной статье про union types.

### 10. Мост к следующей статье: суммы с данными
```scala
// Пока мы рассматривали только "пустые" case:
enum Status:
  case Active
  case Inactive

// А что если нужны данные?
enum StatusWithInfo:
  case Active
  case Inactive(reason: String)  // ← есть параметр!

// Это уже ADT (Algebraic Data Type)
// Алгебраически: 1 + String
// Об этом — в следующей статье
```

- Переход от чистых сумм к суммам с данными
- Анонс следующей статьи об ADT
- Упоминание Option, Either, Result как примеры

### 11. Заключение
- Резюме: чистые суммы = фиксированное множество вариантов без данных
- Алгебраическая природа: кардинальность = сумма
- Главное преимущество: exhaustiveness checking
- Это основа для понимания ADT

## Требования к стилю

### Тон
- Дружелюбный, но технически точный
- Объясняй сложные концепции через простые примеры
- Используй аналогии и визуализацию

### Примеры кода
- Все примеры должны компилироваться
- Используй синтаксис Scala 3
- Комментарии к неочевидным моментам
- Покажи результат выполнения где уместно

### Форматирование
- Используй заголовки разных уровней для структуры
- Блоки кода с синтаксической подсветкой (```scala)
- Выделяй важные моменты (✅ ❌ ⚠️ 💡)
- Таблицы для сравнений

### Алгебраические формулы
- Всегда показывай алгебраическую запись рядом с кодом
- Объясняй обозначения (|A| = кардинальность типа A)
- Связывай с конкретными числами

### Связь с другими статьями
- Ссылайся на статью о произведениях типов
- Анонсируй статью об ADT
- Упоминай, что union types — отдельная тема

## Специальные акценты

### Подчеркни:
1. **Конечность**: чистые суммы = фиксированное конечное множество
2. **Exhaustiveness**: компилятор помогает не забыть случаи
3. **Типобезопасность**: невозможны некорректные значения
4. **Алгебраичность**: точная формула для подсчёта значений
5. **Отсутствие данных**: это ключевое отличие от ADT

### Избегай:
- Слишком абстрактных объяснений без примеров
- Углубления в теорию категорий (это не академическая статья)
- Смешивания с ADT (это следующая статья)
- Сравнения с монадами и функторами (слишком рано)

## Длина статьи
- Примерно 2000-2500 слов
- 10-15 примеров кода
- 2-3 таблицы сравнений
- Время чтения: 10-12 минут

## Дополнительные секции (опционально)

### FAQ
- "Чем enum отличается от sealed trait?"
- "Почему нельзя использовать обычные константы?"
- "Когда использовать enum vs union type?"

### Упражнения для читателя
```scala
// Задание 1: Реализуйте enum для карточных мастей
// Задание 2: Добавьте exhaustive pattern matching
// Задание 3: Вычислите кардинальность типа
```

## Финальная проверка

Статья должна отвечать на вопросы:
- ✅ Что такое чистая сумма типов?
- ✅ Почему называется "суммой"?
- ✅ Как использовать enum в Scala 3?
- ✅ Чем отличается от произведений?
- ✅ Когда применять?
- ✅ Как связано с алгеброй типов?
- ✅ Что будет дальше (переход к ADT)?

Начинай написание статьи!