# Module: Course Indexing Workflow

## Purpose

Определяет пошаговый процесс индексации контента курса.
Практическое руководство для индексатора курса.

## Specification

### Механизм отслеживания состояния с персистентностью

**Критически важно:** На протяжении всего процесса индексации поддерживается структура `indexing_state`, которая:

- Отслеживает какие модули обработаны, какие нет
- **Сохраняется на диск** после каждой итерации в `indexes/.indexing-state.json`
- **Позволяет возобновить** процесс при неожиданном прерывании
- **Автоматически удаляется** после успешного завершения

**Преимущества персистентности:**
- Защита от потери прогресса при сбоях/прерываниях
- Возможность возобновления с места остановки
- Не нужно повторно обрабатывать уже обработанные модули
- Прозрачность текущего состояния (можно посмотреть файл)

**Структура состояния:**
```javascript
indexing_state = {
  version: "1.0",                          // Версия формата состояния
  started_at: "YYYY-MM-DD HH:MM:SS",       // Когда началась индексация
  last_updated: "YYYY-MM-DD HH:MM:SS",     // Последнее обновление
  total_modules: число,                    // Общее количество модулей
  processed_modules: [строки],             // Список обработанных модулей
  pending_modules: [строки],               // Список необработанных модулей
  current_iteration: число,                // Номер текущей итерации
  iterations_plan: [{                      // План итераций
    iteration: число,
    modules: [строки]
  }],
  accumulated_data: {                      // Накопленные данные
    modules: [объекты]
  }
}
```

**Файл состояния:** `indexes/.indexing-state.json`
- Префикс `.` означает технический файл (скрыт в некоторых системах)
- Формат JSON для удобства чтения и отладки
- Обновляется после каждой успешной итерации

---

### Фаза 1: Анализ структуры курса и проверка состояния

**Цель:** Понять объём работы, проверить незавершённую индексацию, спланировать итерации

**Шаг 0. Проверка существующего состояния**

**ПЕРВОЕ ДЕЙСТВИЕ:** Проверь наличие файла состояния:

```bash
ls indexes/.indexing-state.json
```

**Если файл существует:**

1. Прочитай файл и распарси JSON
2. Покажи информацию о незавершённой индексации:

```
🔄 Обнаружена незавершённая индексация!

📊 Состояние:
- Начата: [started_at]
- Последнее обновление: [last_updated]
- Обработано модулей: X из Y
  ✅ Обработанные: [список из processed_modules]
  ⏳ Ожидают: [список из pending_modules]
- Текущая итерация: N

❓ Действия:
1. Возобновить индексацию с места остановки
2. Начать заново (текущий прогресс будет утерян)
```

3. **ОБЯЗАТЕЛЬНО спроси пользователя** что делать:
   - Если "возобновить" → загрузи `indexing_state` из файла и переходи к Фазе 2
   - Если "начать заново" → удали файл состояния и продолжи обычный процесс

**Если файла НЕТ:**

Продолжай обычный процесс создания нового состояния.

---

**Шаги (для нового процесса или при "начать заново"):**

1. **Сканирование директорий**
   - Выполни `ls content/` чтобы получить список курсов
   - Для каждого курса: получи список модулей
   - Для каждого модуля: получи список уроков
   - Подсчитай общее количество `.md` файлов

2. **Планирование итераций**
   - Раздели модули на группы по 2-3 модуля
   - Первая итерация: 1-2 модуля для проверки
   - Последующие итерации: 2-3 модуля
   - Определи общее количество итераций

3. **Создание плана и структуры отслеживания**

   Создай структуру отслеживания состояния:

   ```javascript
   indexing_state = {
     version: "1.0",
     started_at: new Date().toISOString(),
     last_updated: new Date().toISOString(),
     total_modules: X,
     processed_modules: [],
     pending_modules: ["1_Введение", "2_Основы", ...],
     current_iteration: 0,
     iterations_plan: [
       {iteration: 1, modules: ["1_Введение"]},
       {iteration: 2, modules: ["2_Основы", "3_Функции"]},
       ...
     ],
     accumulated_data: {
       modules: []
     }
   }
   ```

   **Сохрани состояние на диск:**
   ```bash
   # Создай файл состояния
   Write(
     file_path: "indexes/.indexing-state.json",
     content: JSON.stringify(indexing_state, null, 2)
   )
   ```

   Покажи план:
   ```
   📋 План индексации создан и сохранён:
   - Всего модулей: X
   - Итераций: Y
   - Итерация 1: Модули ["1_Введение"]
   - Итерация 2: Модули ["2_Основы", "3_Функции"]
   ...

   📊 Статус:
   - Обработано: 0 из X модулей
   - Осталось: X модулей

   💾 Состояние сохранено в: indexes/.indexing-state.json
   ```

### Фаза 2: Поэтапная обработка модулей

**Цель:** Извлечь метаданные из файлов без превышения лимитов контекста

**Для каждой итерации:**

**Шаг 0. Проверка состояния перед итерацией**

Перед началом итерации проверь:
```
📊 Итерация N:
- Планируется обработать: ["Модуль_X", "Модуль_Y"]
- Уже обработано: ["Модуль_A", "Модуль_B", ...]
- Осталось после этой итерации: ["Модуль_Z", ...]
```

Обнови `indexing_state.current_iteration = N`

**Шаг 1. Сканирование файлов модулей текущей итерации**

Для каждого модуля из `iterations_plan[current_iteration].modules`:
- Получи список всех `.md` файлов в модуле
- Исключи `course_info.md` и `README.md`
- Сортируй по пути (модуль → урок → шаг)

**Шаг 2. Извлечение метаданных**

Для каждого файла извлеки:
- ID шага (из имени файла: `5069971_1_text.md` → `5069971`)
- Позицию (из имени файла: `5069971_1_text.md` → `1`)
- Тип контента (из имени файла: `5069971_1_text.md` → `text`)
- Заголовок (первый H1 из содержимого)
- Относительный путь от корня проекта

**Шаг 3. Анализ содержимого (для ключевых слов)**

Прочитай содержимое файлов:
- Извлеки первые 2-3 абзаца
- Определи основные темы
- Выдели ключевые термины (без стоп-слов)
- Сохрани для тематического индекса

**Шаг 4. Накопление данных и обновление состояния**

Добавь извлечённые данные в `indexing_state.accumulated_data.modules`:
```
indexing_state.accumulated_data.modules.push({
  name: "1_Введение",
  lessons: [
    {
      name: "1_Обзор_языка",
      steps: [
        {
          id: 5069971,
          position: 1,
          type: "text",
          title: "Scala: язык программирования",
          path: "content/.../5069971_1_text.md",
          topics: ["история", "Мартин Одерски"],
          keywords: ["Scala", "JVM", "EPFL"]
        },
        ...
      ]
    }
  ]
})
```

**Критически важно:** После обработки каждого модуля обнови состояние:
```javascript
// Для каждого обработанного модуля:
indexing_state.processed_modules.push("1_Введение")
indexing_state.pending_modules = indexing_state.pending_modules.filter(m => m !== "1_Введение")
indexing_state.last_updated = new Date().toISOString()
```

**Шаг 5. Сохранение состояния после итерации**

**КРИТИЧЕСКИ ВАЖНО:** После КАЖДОЙ завершённой итерации сохрани состояние:

```javascript
// Обнови временную метку
indexing_state.last_updated = new Date().toISOString()

// Сохрани на диск
Write(
  file_path: "indexes/.indexing-state.json",
  content: JSON.stringify(indexing_state, null, 2)
)
```

**Это защищает от потери прогресса!** Если процесс прервётся, можно будет возобновить с этого места.

**Шаг 6. Отчёт о прогрессе и проверка состояния**

После каждой итерации покажи:
```
📊 Прогресс индексации (Итерация N завершена):
- Обработано модулей: X из Y
  ✅ Обработанные: ["1_Введение", "2_Основы", ...]
  ⏳ Ожидают: ["3_Функции", "4_Практика", ...]
- Обработано уроков: X
- Обработано шагов: X
```

**Проверка перед переходом к следующей итерации:**
- Все ли модули текущей итерации в `processed_modules`?
- Нет ли дубликатов в `processed_modules`?
- Совпадает ли количество обработанных модулей с ожидаемым?

### Фаза 3: Генерация индексных файлов

**Цель:** Создать все 4 типа индексов на основе накопленных данных

**ВАЖНО: Проверка полноты перед генерацией**

Перед началом генерации индексов ОБЯЗАТЕЛЬНО проверь:

```
✅ Проверка готовности к генерации:
- Всего модулей в курсе: X
- Обработано модулей: Y
- Осталось необработанных: Z

Список обработанных: ["1_Введение", "2_Основы", ...]
Список необработанных: []
```

**Условие продолжения:**
- `indexing_state.processed_modules.length === indexing_state.total_modules`
- `indexing_state.pending_modules.length === 0`

**Если проверка НЕ пройдена:**
```
❌ ОШИБКА: Не все модули обработаны!
- Ожидалось: X модулей
- Обработано: Y модулей
- Не обработаны: [список]

Невозможно продолжить генерацию индексов.
```

**Если проверка пройдена, продолжай генерацию:**

**Последовательность генерации:**

**Шаг 1. module-hierarchy.md**

```markdown
# Иерархия курса

<!--
Сгенерировано: [дата] [время]
Модулей: X
Уроков: X
Шагов: X
-->

## Модуль 1: [Название]

### 1.1 [Название урока]

- Шаг 1: [text] [Заголовок]
  - ID: [id]
  - Путь: [path]
  - Темы: [topics]

Статистика урока:
- Всего шагов: X
- Теория (text): X
- Практика (code): X
- Тесты (choice): X
```

**Шаг 2. topics-keywords.md**

```markdown
# Тематический индекс курса

<!--
Сгенерировано: [дата] [время]
Ключевых слов: X
-->

## Алфавитный указатель

[А](#а) [Б](#б) ... [Я](#я) | [A](#a) ... [Z](#z)

## А

### Akka
- content/.../file.md: "Заголовок шага"

### Apache Spark
- content/.../file.md: "Заголовок шага"
```

Процесс:
1. Собери все ключевые слова из накопленных данных
2. Сгруппируй по первой букве (кириллица → латиница)
3. Отсортируй внутри групп
4. Создай якоря для навигации

**Шаг 3. quick-reference.md**

```markdown
# Быстрая навигация по курсу

<!--
Сгенерировано: [дата] [время]
Шагов: X
-->

| Модуль | Урок | Шаг | Тип | Описание | ID | Путь |
|--------|------|-----|-----|----------|----|----- |
| 1 | 1.1 | 1 | text | [краткое описание] | 12345 | content/.../file.md |
```

Процесс:
1. Для каждого шага создай строку таблицы
2. Сократи описания до 60 символов
3. Добавь итоговую статистику внизу

**Шаг 4. by-content-type.md**

```markdown
# Индекс по типам контента

<!--
Сгенерировано: [дата] [время]
Типов контента: X
-->

## Теоретические материалы (text)

### Модуль 1: [Название]

1. [Заголовок] (ID: [id])
   - Путь: [path]
   - Темы: [topics]
   - Ключевые слова: [keywords]
```

Процесс:
1. Сгруппируй шаги по типу (text, code, choice, fill-blanks)
2. Внутри типа группируй по модулям
3. Добавь статистику по типам

**Шаг 5. README.md**

Обнови секцию "Статистика":
```markdown
## Статистика

- Модулей: X
- Уроков: X
- Шагов: X
- Дата генерации: YYYY-MM-DD
```

### Фаза 4: Валидация индексов

**Цель:** Убедиться в корректности сгенерированных индексов

**Проверки:**

1. **Валидация путей**
   - Для каждого пути в индексах проверь существование файла
   - Список несуществующих путей должен быть пуст

2. **Валидация ID**
   - Все ID должны быть числовыми
   - Нет дублирующихся ID в одном уроке

3. **Валидация статистики**
   - Подсчитай шаги вручную
   - Сравни с указанной статистикой
   - Проверь процентное соотношение типов

4. **Валидация форматов**
   - Проверь корректность Markdown синтаксиса
   - Проверь корректность таблиц
   - Проверь якоря в тематическом индексе

5. **Валидация размера**
   - Проверь размер каждого индекса
   - Если > 100 KB - предупреди пользователя

**Отчёт о валидации:**

```
✅ Валидация индексов пройдена:
- Проверено путей: X (все существуют)
- Проверено ID: X (все корректны)
- Статистика: совпадает
- Форматы: корректны
- Размер индексов: X KB (норма)

❌ Найдены ошибки:
- [список ошибок]
```

### Фаза 5: Финализация

**Цель:** Завершить индексацию и подготовить результаты

**Шаги:**

1. **Создание резервных копий**
   - Если индексы уже существовали
   - Переименовать в формат `[name]-backup-YYYYMMDD.md`
   - Сохранить только 3 последние версии

2. **Сохранение новых индексов**
   - Записать все 4 индексных файла в `indexes/`
   - Обновить `indexes/README.md`
   - Проверить корректность записи

3. **Удаление файла состояния**

   **КРИТИЧЕСКИ ВАЖНО:** После успешного завершения удали файл состояния:

   ```bash
   rm indexes/.indexing-state.json
   ```

   **Причина:** Файл состояния нужен только для возобновления незавершённой индексации. После успешного завершения он больше не нужен и может вводить в заблуждение при следующей индексации.

4. **Итоговый отчёт с отслеживанием**

```
✅ Индексация завершена успешно!

📊 Процесс выполнения:
- Итераций выполнено: Y
- Модулей обработано: X из X ✅

  Итерация 1: ["1_Введение"] ✅
  Итерация 2: ["2_Основы", "3_Функции"] ✅
  Итерация 3: ["4_Практика", "5_Контейнеры", "6_Типы"] ✅

📊 Статистика контента:
- Уроков обработано: X
- Шагов обработано: X
- Теория (text): X (XX%)
- Практика (code): X (XX%)
- Тесты (choice): X (XX%)
- Интерактив (fill-blanks): X (XX%)

📁 Созданные индексы:
- indexes/module-hierarchy.md (X KB)
- indexes/topics-keywords.md (X KB)
- indexes/quick-reference.md (X KB)
- indexes/by-content-type.md (X KB)
- indexes/README.md (обновлён)

📅 Дата индексации: YYYY-MM-DD HH:MM:SS

✅ Все проверки пройдены
✅ Все X модулей обработаны
✅ Состояние отслеживания: processed_modules.length = total_modules
🗑️ Файл состояния удалён: indexes/.indexing-state.json
```

### Чеклист завершения индексации

Перед финализацией убедись:

**Проверка состояния отслеживания:**
- [ ] `indexing_state.processed_modules.length === indexing_state.total_modules`
- [ ] `indexing_state.pending_modules.length === 0`
- [ ] Нет дубликатов в `processed_modules`
- [ ] Каждый модуль курса присутствует в `processed_modules`

**Проверка данных:**
- [ ] Метаданные извлечены для всех шагов
- [ ] Все модули присутствуют в `accumulated_data`
- [ ] Статистика корректна (подсчитана из accumulated_data)

**Проверка индексов:**
- [ ] Все 4 индексных файла созданы
- [ ] README.md обновлён
- [ ] Валидация пройдена без ошибок
- [ ] Размеры индексов в допустимых пределах
- [ ] Резервные копии созданы (если были старые индексы)

**Финализация:**
- [ ] Итоговый отчёт показан пользователю с полным отслеживанием
- [ ] В отчёте указаны все итерации и обработанные модули
- [ ] Файл состояния `indexes/.indexing-state.json` удалён

## Version

* Version: 1.2
* Created: 2025-12-28
* Updated: 2025-12-28
* Status: Active
* Notes: Добавлена персистентность состояния в файл .indexing-state.json с возможностью возобновления после прерывания
