# Module: Indexing Rules

## Purpose

Определяет правила, ограничения и требования к процессу индексации контента курса.
Обеспечивает качество и консистентность индексных данных.

## Specification

### 1. Правила обработки файлов

**Обязательные требования:**

- ✅ Обрабатывать ТОЛЬКО файлы с расширением `.md` в директории `content/`
- ✅ Игнорировать файлы `course_info.md` и `README.md`
- ✅ Извлекать ID шага из имени файла (первое число перед подчёркиванием)
- ✅ Определять тип шага по суффиксу имени файла (`_text`, `_code`, `_choice`, `_fill-blanks`)

**Запрещено:**

- ❌ Обрабатывать файлы вне директории `content/`
- ❌ Изменять исходные файлы контента
- ❌ Пропускать файлы без анализа причины
- ❌ Создавать индексы для несуществующих файлов

### 2. Правила извлечения метаданных

**Заголовок шага:**

- Извлекать первый заголовок H1 (`# ...`) из содержимого файла
- Если H1 не найден - использовать "Без названия"
- Удалять HTML-теги из заголовков
- Сохранять форматирование Markdown (кроме HTML)

**Темы и ключевые слова:**

- Анализировать первые 2-3 абзаца содержимого
- Извлекать существенные термины (не стоп-слова)
- Для тематического индекса - выделять основные концепции
- Группировать синонимы под одним ключевым словом

**Пути к файлам:**

- ВСЕГДА использовать относительные пути от корня проекта
- Формат: `content/{course}/{module}/{lesson}/{file}.md`
- Использовать прямые слэши `/` (не обратные `\`)
- НЕ включать префикс `./` или `../`

### 3. Правила поэтапной обработки

**Критическое требование:** НЕ обрабатывать весь курс за один раз

**Контекст выполнения:**

Индексация ВСЕГДА выполняется в отдельном агенте (через Task tool), чтобы:
- Не перегружать основной контекст разговора
- Иметь изолированное пространство для обработки большого объёма файлов
- Возможность показывать прогресс без захламления основной сессии

**Обязательный процесс:**

1. **Первая итерация:** 1-2 модуля максимум
2. **Последующие итерации:** по 2-3 модуля
3. **Показывать прогресс** после каждой итерации
4. **Накапливать данные** между итерациями
5. **Финальная генерация** только после обработки всех модулей

**Формат отчёта о прогрессе:**

```
📊 Прогресс индексации:
- Обработано модулей: X из Y
- Обработано уроков: X
- Обработано шагов: X
- Текущий модуль: [название]
```

**Причины поэтапности:**
- Предотвращение превышения лимитов контекста LLM
- Возможность отслеживать прогресс
- Ранее обнаружение проблем

### 4. Правила компактности индексов

**Целевые размеры:**

- Один индексный файл: до 100 KB
- Общий размер всех индексов: до 500 KB
- Если превышено - применять стратегии сжатия

**Стратегии оптимизации размера:**

1. Сокращать описания шагов (до 60 символов для таблиц)
2. Убирать дублирование информации между индексами
3. Использовать сокращения для типов (`T` вместо `text` в таблицах - НЕТ, нарушает читаемость)
4. Группировать похожие темы под общим названием

**Правило приоритета:** Информативность > Компактность > Детальность

### 5. Правила валидации индексов

**Обязательные проверки после генерации:**

- ✅ Все пути ведут на существующие файлы
- ✅ Все ID шагов корректны (числовые)
- ✅ Нет дублирующихся записей
- ✅ Статистика совпадает с фактическими данными
- ✅ Алфавитный порядок соблюдён (для тематического индекса)
- ✅ Синтаксис Markdown корректен

**Действия при обнаружении ошибок:**

1. Зафиксировать все найденные ошибки
2. Показать список ошибок пользователю
3. НЕ генерировать индекс с ошибками
4. Предложить исправления

### 6. Правила работы с типами контента

**Типы шагов:**

- `text` - теоретический материал
- `code` - практическое задание с кодом
- `choice` - тест с выбором ответа
- `fill-blanks` - задание на заполнение пропусков

**Определение типа:**

1. Проверить суффикс имени файла (`5069971_1_text.md` → `text`)
2. Если суффикс не найден - анализировать содержимое
3. Если тип не определён - использовать `unknown` и сообщить

**Группировка в индексах:**

- Порядок секций: text → code → choice → fill-blanks → unknown
- Внутри типа: сортировка по модулю → урок → шаг
- Показывать статистику по каждому типу

### 7. Правила обработки ключевых слов

**Извлечение ключевых слов:**

- Приоритет: термины Scala, концепции ФП, названия технологий
- Игнорировать стоп-слова (и, в, на, для, the, and, etc.)
- Выделять термины в кавычках или выделенные жирным
- Сохранять оригинальный регистр (Scala, JVM, а не scala, jvm)

**Нормализация:**

- Приводить синонимы к единой форме
  - "функция высшего порядка" = "функции высших порядков"
  - "pattern matching" = "сопоставление с образцом"
- Группировать связанные темы
- НЕ изменять устоявшиеся термины (trait, case class)

**Алфавитная сортировка:**

- Сначала кириллица (А-Я), потом латиница (A-Z)
- Игнорировать регистр при сортировке
- Цифры в конце алфавита

### 8. Правила обновления индексов

**Когда пересоздавать индексы:**

- После добавления новых модулей/уроков/шагов
- После изменения структуры курса
- После переименования файлов
- По запросу пользователя

**Инкрементальное обновление - НЕ ПОДДЕРЖИВАЕТСЯ**

Причина: Риск несогласованности данных. Всегда пересоздавать полностью.

**Версионирование:**

- Сохранять старые индексы с суффиксом `-backup-YYYYMMDD`
- Хранить не более 3 последних версий
- Документировать изменения в README.md

### 9. Критерии качества индексации

**Индексация считается успешной если:**

- ✅ Обработаны ВСЕ модули курса
- ✅ ВСЕ 4 типа индексов сгенерированы
- ✅ Валидация пройдена без ошибок
- ✅ Статистика корректна
- ✅ README.md обновлён
- ✅ Размер индексов в допустимых пределах

**Критерии отказа от индексации:**

- ❌ Невозможно прочитать более 20% файлов
- ❌ Структура директорий не соответствует ожидаемой
- ❌ Критические ошибки в метаданных

### 10. Правила работы с файлом состояния

**Файл состояния:** `indexes/.indexing-state.json`

**Назначение:**
- Сохранение прогресса индексации между итерациями
- Возможность возобновления после неожиданного прерывания
- Отслеживание какие модули обработаны, какие нет

**Обязательные правила:**

1. **Проверка при старте:**
   - ВСЕГДА проверяй наличие файла перед началом индексации
   - Если файл существует - ОБЯЗАТЕЛЬНО предложи пользователю возобновить или начать заново
   - НЕ игнорируй существующий файл состояния

2. **Сохранение состояния:**
   - Сохраняй состояние ПОСЛЕ КАЖДОЙ успешной итерации
   - Обновляй `last_updated` при каждом сохранении
   - Используй формат JSON с отступами (для читаемости)

3. **Возобновление:**
   - При возобновлении загружай полное состояние из файла
   - Проверяй версию формата (`version` field)
   - Валидируй целостность данных перед продолжением

4. **Удаление:**
   - ОБЯЗАТЕЛЬНО удаляй файл состояния после успешного завершения
   - НЕ удаляй файл при ошибках (позволит возобновить)
   - Удаляй файл при выборе "начать заново"

**Формат файла:**

```json
{
  "version": "1.0",
  "started_at": "YYYY-MM-DD HH:MM:SS",
  "last_updated": "YYYY-MM-DD HH:MM:SS",
  "total_modules": число,
  "processed_modules": ["строки"],
  "pending_modules": ["строки"],
  "current_iteration": число,
  "iterations_plan": [объекты],
  "accumulated_data": {объект}
}
```

**Обработка ошибок:**

- Если файл повреждён (невалидный JSON) - предупреди и предложи удалить
- Если версия формата не совпадает - предупреди и предложи начать заново
- Если данные неконсистентны - предупреди и предложи начать заново

### 11. Правила использования инструментов

**КРИТИЧЕСКИ ВАЖНО:** Используй ТОЛЬКО встроенные инструменты Claude Code

**Запрещено создавать вспомогательные скрипты:**

- ❌ НЕ создавай Python-скрипты (.py файлы)
- ❌ НЕ создавай Node.js скрипты (.js файлы)
- ❌ НЕ создавай shell-скрипты (.sh файлы)
- ❌ НЕ создавай промежуточные JSON файлы (кроме .indexing-state.json)
- ❌ НЕ создавай любые другие вспомогательные исполняемые файлы

**Причины запрета:**
- Добавляет внешние зависимости (Python, Node.js)
- Создаёт мусорные файлы в indexes/
- Усложняет процесс и отладку
- Не соответствует архитектуре системы

**Обязательно используй встроенные инструменты:**

- ✅ **Read** - для чтения файлов курса
- ✅ **Glob** - для поиска файлов по паттернам
- ✅ **Grep** - для поиска по содержимому
- ✅ **Write** - для создания индексных файлов
- ✅ **Bash** - только для простых команд (ls, rm, mkdir)

**Накопление данных:**
- Храни `indexing_state` в памяти во время работы
- Сохраняй только `.indexing-state.json` для персистентности
- НЕ создавай промежуточные файлы данных

### 12. Правила безопасности

**Запрещённые операции:**

- ❌ НЕ модифицировать файлы в `content/`
- ❌ НЕ удалять исходные файлы
- ❌ НЕ перемещать файлы контента
- ❌ НЕ изменять структуру директорий
- ❌ НЕ удалять файл состояния при ошибках (нужен для возобновления)

**Разрешённые операции:**

- ✅ Читать файлы в `content/` (read-only)
- ✅ Создавать/обновлять файлы в `indexes/` (только .md и .indexing-state.json)
- ✅ Создавать резервные копии индексов
- ✅ Создавать/обновлять `indexes/.indexing-state.json`
- ✅ Удалять `indexes/.indexing-state.json` только после успешного завершения или при "начать заново"

## Version

* Version: 1.3
* Created: 2025-12-28
* Updated: 2025-12-28
* Status: Active
* Notes: Добавлен явный запрет на создание вспомогательных скриптов (Python, Node.js, shell). Обязательно использование только встроенных инструментов Claude Code.
